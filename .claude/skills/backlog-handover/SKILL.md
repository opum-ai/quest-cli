---
name: backlog-handover
version: "0.9.1-qcli.9"
description: "Multi-session burndown campaign driver for this project's Backlog.md queue. Each restore session recomputes the ready set from native Backlog dependencies, drains as many non-conflicting tasks as it safely can — dispatched in parallel waves to isolated worker subagents in git worktrees, gated by a mandatory top-tier reviewer, merged one at a time by the orchestrator — then writes a grounded handover so the next session continues with '/clear' + restore. Explicit invocation only: this is the heavyweight campaign driver, NOT routine Backlog.md work. Ordinary task reads, creates, edits, and single-task execution use the plain `backlog` CLI per CLAUDE.md."
disable-model-invocation: true
---

# Backlog Handover — DAG-parallel campaign driver

Burn down this project's Backlog.md queue across many small sessions, each draining as much as it safely can. Input: $ARGUMENTS

The orchestrator (this session) never implements or reviews anything itself — it computes what is safe to run in parallel, manages worktrees, dispatches subagents, and serializes the shared-state steps (merge, task settlement, campaign-doc writes) that cannot be parallelized. The user drives the whole campaign with only:

```
/clear  →  /backlog-handover restore  →  (repeat until the queue is empty)
```

**This skill is invoked explicitly, never inferred.** "Work through the backlog" in this project usually means ordinary `backlog task` usage, not a parallel campaign — hence `disable-model-invocation: true`.

---

## Requirements

- **Always**: git; the `backlog` CLI (this project's sole system of record — see CLAUDE.md).
- **Optional**: remote `origin` (push/PR/prune steps skipped without one); `gh` authenticated, for PRs; `treehouse` CLI for pooled worktree reuse (plain `git worktree add` is the fallback).
- **Full wave-parallel execution**: the session must be able to dispatch parallel subagents via the Agent tool with per-call model selection (mid-tier workers, top-tier reviewer). Without it, degrade gracefully — see Execution Model.

Read `backlog instructions overview` at the start of every campaign session, per CLAUDE.md. Workers and reviewers are told to read `task-execution` and `task-finalization` respectively.

### Statusline (optional)

ECK is not required by this skill. Guard every call so a non-ECK environment degrades silently:

```bash
[ -n "$ECK_HOME" ] && [ -x "$ECK_HOME/update-stage.sh" ] && bash "$ECK_HOME/update-stage.sh" "Waves (4/6)"
```

Reset with the same command and no argument at completion (success or error).

| Stage | Subject            | Statusline      |
| ----- | ------------------ | --------------- |
| 1     | Stage 1: Locate    | Locate (1/6)    |
| 2     | Stage 2: Verify    | Verify (2/6)    |
| 3     | Stage 3: Reconcile | Reconcile (3/6) |
| 4     | Stage 4: Waves     | Waves (4/6)     |
| 5     | Stage 5: Re-arm    | Re-arm (5/6)    |
| 6     | Stage 6: Report    | Report (6/6)    |

(Restore mode — the driver. Init reuses it as Inventory / Confirm / Create / Handover; Write and Status are single-stage.)

---

## Usage

```bash
/backlog-handover init      # one-time: build the campaign doc from the open queue, write the first handover
/backlog-handover restore   # THE DRIVER: verify ground truth, drain wave after wave, re-arm
/backlog-handover write     # bailout: session ending with work unfinished — write a grounded handover
/backlog-handover status    # read-only: queue partition, campaign doc, handover, branch/worktree state
```

Mode detection: an explicit mode argument wins. Otherwise infer: continue/resume/burn-down language → `restore`; set-up-a-campaign language → `init`; session-ending-with-work-unfinished → `write`. Genuinely bare or ambiguous → `status` — the only safe default when intent cannot be determined, never a way to silently ignore a clear request to act.

---

## Execution Model

Three tiers, fixed roles — do NOT blur them:

| Tier | Who | Does | Never does |
| ---- | --- | ---- | ---------- |
| **Orchestrator** | This session (top-tier model, Agent-tool parallel dispatch) | Computes the ready/conflict graph; creates and manages every worktree; dispatches workers/reviewers; runs the serialized merge queue; performs every task settlement (AC checks, final summary, terminal status) and every campaign-doc write | Implements code, writes a review verdict, edits a task's code inside its worktree. Its only hands-on contact with a task branch is worktree lifecycle and the final merge |
| **Worker** | Mid-tier agent (e.g. `model: sonnet`), dispatched per task via the Agent tool, worktree path given as explicit cwd | One task: read it, plan, implement, self-verify against real gates, record plan/notes/comments on its own task, commit, push | Create or remove its own worktree; merge; check acceptance criteria; write the final summary; move the task to a terminal status; edit the campaign doc; create new tasks |
| **Reviewer** | Top-tier agent (e.g. `model: opus` or strongest available), dispatched into the worker's existing worktree | The **mandatory** gate for every task and the judgment call for every escalation; independently re-runs verification; returns a structured per-criterion verdict | Resolve conflicts or write fixes itself — it decides disposition and hands fixes to a fresh worker; write to Backlog or git itself — the orchestrator records its verdict |

**Degraded mode** (no parallel dispatch, or no per-call model selection): wave size = 1, a single plain feature branch (no worktree management — only one task ever in flight), implement yourself, and run the review as an explicit adversarial self-review pass — the review step still happens, never skipped. **Default to one wave (= one task) unless the user asks for more**: in degraded mode the implementation transcript accumulates in this session's own context, so the old one-task-per-session bound applies again. Every other rule (merge serialization, settlement centralization, escalation criteria, wave-log format) is unchanged — this is the same algorithm at both ends, not two procedures.

Why waves instead of one task per session: the one-task rule existed to stop a single acting model's context from degrading. Here the orchestrator never does the straining work — implementation and review live in fresh subagents whose transcripts never enter its context — so the unit that must stay small is the *wave* (a bounded, conflict-disjoint batch), not the task.

---

## Conventions

| Thing | Convention |
| ----- | ---------- |
| **System of record** | Backlog.md, via the `backlog` CLI **only**. Never edit files under `backlog/` directly (CLAUDE.md). There is no second tracker: no GitHub Issues, no `tracker:*` primitives |
| **Bulk read** | Two-tier, and this is a real constraint: `backlog task list --json` returns id/title/status/labels/ordinal but **not** dependencies, description, or acceptance criteria. Get the roster from one `task list --json`, then `backlog task view <ID> --json` **only for campaign-labelled, non-Done candidates** — never the whole backlog, never once per task in a loop over everything |
| **Campaign membership** | Label `campaign`. Applied at init to every queued task; the ready-set computation only ever considers labelled tasks, so ad-hoc tasks created outside the campaign are never swept into a wave |
| **Dependencies** | Backlog's **native** `dependencies` field (`--dep` at create, `--dep` at edit — note edit *replaces* the list). This is the dependency graph; do not maintain a parallel Deps table |
| **Stage state** | Backlog has three statuses, the campaign has six stages. Status carries the coarse state; the committed record distinguishes four of the six stages — for the other two (`in-review`, `merge-pending`) the label is applied to the working tree and deliberately discarded before it is ever committed (`QCLI-51`), so it carries no sub-stage in Backlog's committed state at all. See the durability column in the state table below for exactly which four vs which two |
| **Campaign doc** | A Backlog document: `backlog doc create "Backlog campaign tracker" -p campaigns -t other`, thereafter `backlog doc update <docId> --content "..."`. Lands under `backlog/docs/`. **Never** in the lore-managed `docs/` tree, and never hand-edited |
| **Handover** | Active: `.claude/handovers/HANDOVER-{YYYY-MM-DD}-backlog-campaign.md` (gitignored). Consumed → `archive/handovers/` (tracked; on name collision suffix `-2`, `-3`, …). One active handover per topic. Conventions are inlined here — this project has no separate `handover` skill |
| **Wave** | The atomic dispatch unit: a conflict-disjoint subset of the ready set, implemented and reviewed in parallel, merged serially |
| **Wave size cap** | Default 6 concurrent workers regardless of ready-set size — bounds worktree/disk cost, reviewer throughput, and how many sequential rebase-merges one drift check must reconstruct if a session dies mid-wave. A wave shrinking to 1 is the algorithm correctly degrading to sequential, not a bug |
| **Worktrees** | Orchestrator-managed. `treehouse` lease when the CLI is present, else `git worktree add`. Full lifecycle in `reference/wave-loop.md` |
| **Feature branch** | `feat/qcli-<N>-<slug>` or `fix/qcli-<N>-<slug>` per change type. Created by the orchestrator as part of worktree setup — the worker is dispatched *into* an already-branched worktree and never runs `git checkout -b` |
| **Default branch** | `dev` (this repo's integration branch). Resolve as `git symbolic-ref --short refs/remotes/origin/HEAD` stripped of `origin/`; fall back to `dev`, then `main`. If that symbolic ref is missing, run `git remote set-head origin -a` once rather than guessing |
| **Commits** | Project commit conventions. Hybrid trailer rule (`QCLI-47`, refining `QCLI-43`'s single exception): a `Refs: QCLI-<N>` trailer is **required** on every commit that has a directing task — every worker/implementation commit; every orchestrator settlement commit (single task); and every orchestrator dispatch-marking or in-flight-pointer-recording commit (one commit per pass, carrying one `Refs: QCLI-<N>` trailer per task marked or recorded in that pass, not one commit per task — `QCLI-49`). Two exceptions carry **no** trailer, both genuinely task-less: `lore sync`'s own `backlog/` auto-commit (hardcoded in the lore binary as `chore(backlog): sync task changes`, outside this skill's control — `QCLI-43`'s original exception), and campaign-scoped bookkeeping commits with no single directing task — campaign init, campaign close, handover archiving, and the one-time `.claude/handovers/` gitignore setup at I3. A `Refs:` line only counts if `git interpret-trailers --parse` reports it as a trailer — it must sit in the message's final trailer block with no blank line separating it from other trailers (e.g. a squash-merge's generated `Co-Authored-By:` block), a failure mode a naive `%(trailers:key=Refs)` check silently misses (`QCLI-48`). Full per-commit-type breakdown, the placement rule, verification command, and worked examples in `reference/wave-loop.md` section i |
| **PR** | Optional audit trail, not a second approval wait. `gh pr merge <branch> --squash --delete-branch` after rebase + re-verify + re-push. **There is no `Closes #N` auto-close for Backlog tasks** — settlement always moves the task to `Done` explicitly. No `gh`/no remote → local `git merge --ff-only` |
| **Review gate** | Mandatory. Every branch needs an `approve` verdict before it is eligible for the merge queue |
| **Write concurrency** | Worker writes to its **own** task (`--plan`, `--append-notes`, `--comment`) are parallel-safe. AC checks, final summary, terminal status, `--dep` edits, and campaign-doc writes are **orchestrator-only and serialized** |

### Campaign stage → Backlog state

| Stage | Status | Labels | Committed to Backlog? |
| ----- | ------ | ------ | ---------------------- |
| Queued | `To Do` | `campaign` | Yes |
| Dispatched | `In Progress` | `campaign`, `wave-<N>` | Yes — committed by (d)'s dispatch-marking pass |
| In review | `In Progress` | `campaign`, `wave-<N>`, `in-review` | **No — working-tree-only, see below** |
| Merge-pending | `In Progress` | `campaign`, `wave-<N>`, `merge-pending` | **No — working-tree-only, see below** |
| Done | `Done` | `campaign`, `wave-<N>` (ACs checked, final summary set) | Yes — committed at settlement, `reference/wave-loop.md` section (i) |
| Blocked / needs human | `To Do` | `campaign`, `needs-human` (reason recorded in the campaign doc) | Yes |

Everything Backlog can express natively lives in Backlog. The campaign doc holds only what it cannot: human-confirmed priority order, cluster assignments, the wave log, in-flight worktree/branch pointers, and needs-human reasons.

**`in-review` and `merge-pending` are never observable in a committed task file** (`QCLI-51`, sharpening `QCLI-49`). `reference/wave-loop.md` section (f) applies each as a `backlog task edit` on the orchestrator's own `<default>` checkout, confirms the resulting diff is label-only, and discards it uncommitted as soon as that task's own review reaches a terminal verdict — before section (g)'s merge walk begins for the wave, not merely before that one task's own later turn within it (`QCLI-53`, correcting a wording gap between here and (g)'s precondition) — so neither label is ever an ancestor of any commit on `<default>`, and a `backlog task view` run against a clean checkout will never show either one, even for a branch that has already been reviewed and approved. Both rows get identical durability treatment for the same reason: committing either mid-wave, while the affected branch is still unmerged, is exactly the rebase conflict `QCLI-49` closed.

The two labels mark different points of action, stated explicitly here rather than left to the trailing "and later" this table used to imply:

- `in-review` is applied when section (f) dispatches the reviewer into the worker's worktree — review's start.
- `merge-pending` is applied when that reviewer's verdict comes back `approve`, before the branch is handed to section (g)'s merge walk — review's end, not an unspecified "later." `reference/wave-loop.md` section (f) names this step directly.

Both are reconstructed correctly once the branch has actually merged: settlement (section i) sets the full label set for a `Done` or `needs-human` task in one pass, on the single surviving copy of the file. Because neither label ever reaches a committed state, R2's crash-recovery classification must not — and does not — cross-check a leftover branch or worktree against either one; see R2 for what it checks instead.

---

## Project rules that override the upstream design

This skill is a fork of an ECK/GitHub-Issues skill. Three of this project's mandated rules change its behaviour — do not "restore" the upstream shape:

1. **No autonomous follow-up tasks.** `backlog instructions task-finalization` states: *do not create or start follow-up tasks without user approval.* Wave-level integration findings and out-of-scope discoveries are therefore **surfaced to the user at R6**, never auto-filed. The orchestrator drafts the proposed task (title, description, ACs) in the campaign doc and the report, and waits.
2. **No archiving of completed work.** Same guide: *tasks in the terminal status stay there until periodic cleanup.* Never run `backlog task archive` or `backlog task complete` on campaign tasks.
3. **Finalization order is fixed.** Verify objectively → `--check-ac <index>` (only what evidence proves) → `--check-dod` → `--append-notes` → `--final-summary` → status `Done`. Never check an AC from code presence, grep output, or implementation intent.

---

## Init Mode

**I1: Inventory.** One `backlog task list --json` for the roster; `backlog task view <ID> --json` for each non-Done candidate. Classify honestly: *agent-resolvable now* (joins the campaign) vs *needs a human / product decision / blocked* (recorded in the campaign doc's needs-human section with the reason, label `needs-human`). A task whose acceptance criteria cannot be objectively verified by an agent alone does not belong in the queue — it only manufactures a stuck wave later. Assign each queued task a one-word cluster (subsystem/topic) as label `cluster:<name>`.

**I2: Confirm with the user.** Propose an order (lowest-risk/highest-information first: doc-only → small code → spikes) and get explicit confirmation via AskUserQuestion. Record it verbatim in the campaign doc. State plainly that it is the wave-builder's tie-break, not a strict execution order.

**I3: Create the campaign doc + directories.** `backlog doc create "Backlog campaign tracker" -p campaigns -t other`, populated from `reference/templates.md`. Apply `campaign` and `cluster:*` labels to queued tasks. Ensure `.claude/handovers/` is gitignored and `mkdir -p archive/handovers`. Commit the gitignore change on the default branch (campaign-scoped, no single directing task — the Commits convention's no-trailer exception, not the `always a trailer` default).

**I4: Write the first handover.** Run Write mode, then tell the user the driver loop: `/clear` → `/backlog-handover restore`.

---

## Restore Mode — the driver

**R1: Locate.** Newest `.claude/handovers/HANDOVER-*-backlog-campaign.md`. No handover but the campaign doc exists → say so and proceed from the doc alone (the handover is an accelerator; Backlog + the campaign doc are the record). Neither exists → suggest `init`; STOP.

**R2: Verify ground truth.** A crashed session may have left branches at *different* lifecycle stages simultaneously. Re-verify everything before acting:

1. `git fetch`; default branch moved past the grounding SHA? Working tree clean? Unpushed commits?
2. `git worktree list --porcelain` **and** `git branch --list 'feat/qcli-*' 'fix/qcli-*'` (local + remote) — enumerate every leftover, not just what the handover mentions. `git worktree add` hard-fails if a branch is already checked out in a stale worktree. With treehouse in play, `treehouse status --json`: a lease whose holder matches this campaign's labels is an in-flight member left by a crashed session — leases survive with zero processes running, so they never show as "in use". A campaign-labelled lease with no matching `In Progress` task is the orphan signal.
3. `gh pr list --state all` for every leftover branch — an open unmerged PR means a session died between opening it and the merge queue.
4. Cross-check every leftover against Backlog state: `In Progress` + `wave-N` means implementation may be mid-flight, done-but-unreviewed, or already reviewed-and-approved — neither review-adjacent label ever commits (`QCLI-51`), so status plus the campaign label alone cannot tell those apart. Check the worktree's own `git log` and step 5's other signals to disambiguate. Classify each as *matches Backlog* (resume at its recorded stage) or *orphaned* (report it, reconcile in R3, do NOT silently delete).
5. **Neither `in-review` nor `merge-pending` will ever be present on a leftover task's committed state** — see the stage-state table's durability column (`QCLI-51`). Do not read either label's absence as "review never started" or "never approved," and do not expect either from a fresh `backlog task view`. Derive the review substage instead from what actually persists: step 1's "working tree clean?" check, applied to the *orchestrator's own* `<default>` checkout — since review is pipelined per completed implementer (f), more than one wave member can be mid-review at once, so a dirty diff there can carry one entry per task file currently mid-(f); a label-only dirty entry for *that specific task's* file means a crash landed mid-(f) for it — but only `in-review`'s appearance still reliably pins that to *before* the branch's `approve` verdict: `QCLI-53` retimed `merge-pending`'s own discard to fire immediately after it is applied and confirmed rather than deferred to this task's own later turn in (g)'s merge queue, which is what makes (g)'s clean-checkout precondition hold by construction, but as a direct cost narrows a caught `merge-pending` entry from routine to rare — treat its *absence* as uninformative (most post-`approve` crashes will see no dirty entry at all, not a `merge-pending` one) rather than reading "no `merge-pending` label seen" as "not yet approved," and corroborate `after-approve` state from the two signals below that do not depend on catching a mid-(f) dirty diff at all — the worktree's own `git log` (step 4), for whether the worker's own stage-4 task-file commit exists, establishes only that implementation was committed, not that review approved it; step 3's `gh pr list`, since (g) opens a PR only for a branch that already carries `approve` — an open PR is durable proof review passed even though `merge-pending` itself leaves no trace, and is now the more load-bearing of the two original non-label signals for exactly the case this narrowing weakens; and a fourth signal, the campaign doc's own **in-flight table** (`reference/templates.md`'s `Stage reached` column) — the one substate record this campaign commits on purpose, since an in-flight-pointer-recording pass is committed immediately and never left dirty (`reference/wave-loop.md` section (i)'s scope note), unlike (f)'s label edits. A recorded `Stage reached` is written free-form by the orchestrator and can over-report: doc-10 wave 2's `0b63077` recorded `6 — under review` for `chore/qcli-43-settlement-log-sync` when the branch had in fact only reached stage 5 (pushed, review not yet run) — a prior session had implemented, committed, and pushed it, then died before the review gate ran. Doc-10's own settled campaign doc later corrects that same row inline: "...Backlog state and worktree state agreed, so R3 resumed it at its recorded stage (5, pushed) rather than restarting." Read the row's *stated annotation*, not the bare numeral, and corroborate any claimed post-dispatch stage against the other three signals above before treating a leftover branch as reviewed or approved — a numeral alone is not proof. Five in-flight-pointer-recording commits exist in this repo's history as of this writing (`git log dev --format='%h %s' | grep -i "in-flight pointer"`: `82fca71`, `68ce681`, `61d48af`, `0b63077`, `146956d`); four record stage 1 (dispatch), and the fifth is `0b63077` itself — the over-reporting example above, not a clean confirmation that the signal reports later stages reliably. Both directions of error are real here, not just one: an entry can under-report (nothing mandates a further recording pass at every later transition, so a stale stage-1 entry does not prove progress stalled) and, per `0b63077`, it can equally over-report. Treat a recorded stage past dispatch as a lead to corroborate against the first three signals, never as freestanding proof either way; fall back to those three signals whenever corroboration is missing or conflicts with the row. A leftover `In Progress` + `wave-N` branch with none of these four signals showing later progress is presumptively pre-approval; resume it at review rather than guessing a later stage it cannot prove.

Produce a short drift table (`claim → record said → now`). If drift invalidates the plan, adapt and say so — never execute stale instructions.

**R3: Reconcile.** Completed-but-unrecorded work found in R2 goes into Backlog (notes, ACs where evidence exists, final summary, status) and the campaign doc's wave log before any new wave starts. A leftover worktree matching an `In Progress` task resumes from that task's recorded stage rather than restarting. Reconciliation also re-runs the lore log sync: `reference/wave-loop.md` section i's crashed-session failure path is mandatory here — re-run `lore sync` before starting a new wave, and if it reports any `files`, gate with `lore check --strict` and commit/push them before proceeding. A prior session may have died mid-sync, and R3 is where that gets caught, not the next settlement. Reconciliation must also retry a dispatch-marking or in-flight-pointer-recording push that R2 step 1 found missing: `reference/wave-loop.md` (d) step 4's push and its non-fast-forward recovery apply here too — an unpushed commit found at this point is not a benign gap, it means the push failed or never ran, and R3 must retry it (subject to that same recovery) before treating the wave as resumable (`reference/wave-loop.md` (d) step 4, `QCLI-60`).

**R4: The wave loop — drain until done or blocked.** Full mechanics in `reference/wave-loop.md`. In brief: compute the dependency and conflict graphs → acquire worktrees → mark the acquired members dispatched → implement in parallel → review pipelined per completed implementer → merge strictly serially with rebase and mandatory re-verification → wave-level integration review → settle every task, then sync the lore log (`lore sync` gated by `lore check --strict`, once per wave, last — reference/wave-loop.md section i) → loop or stop.

The orchestrator's context grows only by a roughly constant per-wave increment (dispatch prompts, terse structured returns, verdicts, merge SHAs, one doc delta). This does **not** hold in degraded mode, hence its one-wave budget.

**R5: Re-arm** (once, when the loop terminates).

1. Archive the consumed handover to `archive/handovers/` (collision suffix `-2`, `-3`, …); commit on the default branch (session/campaign bookkeeping, no single directing task — no `Refs:` trailer, per the Commits convention's exception).
2. Write one fresh handover reflecting the session's *cumulative* state across all waves (Write mode) — unless the queue is now empty, in which case R6's campaign-complete handling applies (archive only, no new handover).
3. `git push origin <default>` — unconditional; the archive commit is new even when the wave loop already pushed (no remote → skip).

**R6: Report.** Summarize every task resolved this session, grouped by wave, with evidence and merged SHAs. **Put escalations and needs-human items first and visually distinct** — those are the only things needing the user's attention. Then, separately, **propose** any follow-up tasks the integration review surfaced and ask whether to create them (never create unprompted). State queue counts (resolved / in-flight / blocked / ready-now) and end with the literal next command: `/clear` then `/backlog-handover restore`.

**Queue empty instead?** Campaign complete: summarize resolved work across every wave, archive the final handover (no new one), note completion in the campaign doc, and suggest `init` for a fresh queue. Do not archive the tasks themselves.

---

## Write Mode (bailout / init's final stage)

**W1: Ground truth.** Verify with commands, never memory: current branch + HEAD SHA, `git status --porcelain`, unpushed commits, **every** branch/worktree/PR touched this session, campaign-doc state.

**W2: Flush durable facts first.** Implementation decisions and evidence → task notes/comments. Reviewer verdicts → PR bodies and the wave log. Campaign state → the campaign doc. The handover holds pointers, not facts.

**W3: Write the handover** using the template in `reference/templates.md`. No invented content: every SHA and status verified in W1, gaps stated as gaps. Failed approaches are mandatory when anything failed. **Never persist a "next wave" plan** — the next restore recomputes the ready set live, and a stale plan is worse than none. No secrets or machine-specific paths in anything committed.

**W4: Confirm.** Output the path, waves/tasks resolved this session, and the driver-loop reminder.

---

## Status Mode

Read-only: campaign doc, full queue partition (Done count, in-flight with per-branch/worktree stage, blocked/needs-human, ready-now count), active handover file(s), every campaign branch, `git worktree list` (plus `treehouse status --json` when in play), open PRs, default-branch ahead/behind, dirty files.

Under wave-parallel execution, **several simultaneous branches, worktrees, and open PRs mid-wave are the expected steady state**, not a violation. The actual anomaly is a branch/worktree/PR with no corresponding `In Progress` campaign task — flag it, with the fix (reconcile per R3, or clean up if truly abandoned).

---

## Reference

- `reference/wave-loop.md` — R4 in full: graph computation, worktree lifecycle, dispatch prompts, review stage, the serialized merge queue, settlement, stop conditions
- `reference/escalation.md` — escalation policy and the error-handling table
- `reference/templates.md` — campaign-doc skeleton and handover template

## Provenance

Forked from `opum-doc` `.claude/skills/backlog-handover` v0.9.1-ocli.1, which itself forked `salient-data/skadilabs` `.claude/skills/backlog-handover` v0.9.1 and rebound it from GitHub Issues onto Backlog.md (removing ECK primitive references, `.claude/project-constants.md`, and the companion `handover`/`tracker` skills; inlining their conventions; fixing the upstream dispatch-marking-before-worktree-acquisition defect; removing autonomous follow-up filing and archive-on-complete). This copy's first local change (`0.9.1-qcli.1`) was the task-ID/branch/lease prefix rebind from `OCLI`/`ocli` to `QCLI`/`qcli` for this repo's Backlog project (`task_prefix: qcli` in `backlog/config.yml`) — no other behavioral change at that point.

**Deliberate divergence (`0.9.1-qcli.2`, `QCLI-43`, 2026-08-07):** upstream settlement (R4i) ends at the campaign-doc write. This fork adds a mandatory `lore sync` step, gated by `lore check --strict`, to the end of every wave's settlement (`reference/wave-loop.md` section i), so `docs/log.md` and the bundle's managed blocks are current as of that wave's own settlement instead of drifting further with every subsequent one — closing a drift that had reopened repeatedly (QCLI-35, QCLI-39, and again before this task) for two distinct reasons, not one: a squash-rewrite mechanism that fired repeatedly and left dangling SHAs — measured across six episodes (QCLI-16, QCLI-17, QCLI-19, QCLI-21, QCLI-22, QCLI-28), closed for good by QCLI-35's fix and never recurring since (0 bad of 86 immediately after QCLI-35's merge, 0 bad of 92 immediately after QCLI-39's, 0 bad through the current 95 recorded SHAs) — and ordinary staleness — new `docs/`-touching commits landing with no sync run — which is what actually drove every reopening after that and is the mode this per-wave contract targets. Upstream has no lore/OKF docs bundle and nothing analogous to drift, so this is not a gap upstream needs closed — it is specific to this repo's docs tooling and is not proposed for promotion upstream. See `QCLI-43` for the full analysis, including why running the sync from settlement (on `<default>`, strictly after that wave's merges land, never on a per-task branch — a constraint this skill already enforced for unrelated reasons) is what makes the recorded SHAs permanent `<default>` ancestors instead of doomed branch commits, and why "catches up" means "current as of that wave's settlement commit" rather than a literal same-session zero-lag guarantee — the sync commit itself touches `docs/` and so cannot record itself; see `reference/wave-loop.md` section i.

**Deliberate divergence (`0.9.1-qcli.3`, `QCLI-47`, 2026-08-07):** the Commits convention above was `QCLI-43`'s single exception — always a trailer except `lore sync`'s own `backlog/` auto-commit — but the doc-10 wave-2 reviewer found the orchestrator's own campaign bookkeeping commits routinely lack the trailer in practice: a rule naming exactly one exception sitting next to practice with more than one. Verified 2026-08-07 at doc-11 init that practice is inconsistent, not uniformly absent, correcting how doc-10 first framed it: `%(trailers:key=Refs)` is empty on `8721feb`, `146956d`, `9c63769`, `d0b5f41`, `3686859`, `34bceae`, `8caae19`, `748bf5f`, `6047774`, but carries `Refs: QCLI-43` on `0b63077` and `342e76d`. The split tracks a real distinction, not noise: single-task bookkeeping commits (dispatch marking, in-flight pointer recording, settlement) increasingly do carry the trailer, while genuinely campaign-scoped commits (init, close, handover archiving, the one-time gitignore setup) do not, because they have no single directing task to name. The owner ruling, obtained at doc-11 campaign init before dispatch, is hybrid: keep the trailer required wherever a commit has one directing task — matching what the evidence already shows the orchestrator doing correctly in `0b63077` and `342e76d` — and add campaign-scoped bookkeeping as a second named exception alongside `lore sync`'s auto-commit, rather than discarding the traceability the single-task commits already provide just to make the rule simpler. This is a documentation-only change: no existing commit is amended or re-trailered, and no claim is made about commits predating this ruling. See `QCLI-47` for the full evidence and `reference/wave-loop.md` section i for the per-commit-type breakdown.

**Verification rule added (`0.9.1-qcli.4`, `QCLI-48`, 2026-08-07):** `QCLI-47`'s hybrid rule assumed `Refs:` text in a commit message and a `Refs:` trailer git actually parses were the same thing; doc-11 wave-1 review found they are not. A `Refs: QCLI-<N>` line separated from the message's final trailer block by a blank line — the shape a PR squash-merge produces when its generated body concatenates multiple source commits, each ending its own `Refs:` line, followed by a synthesized `Co-Authored-By:` block — reads as a trailer but is not one: `git interpret-trailers --parse` and `%(trailers:key=Refs)` both silently report nothing for it. This directly threatens `QCLI-47`'s own evidence method, which counts trailers via that same query. Confirmed predating doc-11 and not unique to one commit: `QCLI-47`'s own branch commit `6f3236f` had this shape (caught and fixed at merge time by authoring the squash body explicitly — the merged `694e109` parses correctly), and the already-merged `QCLI-43` squash commit `7efc1a4` has it too. A full sweep of `dev` (258 commits) found 202 carrying `Refs:` text, 36 of those unparseable — command, method, and the full per-commit list are in `reference/wave-loop.md` section i and `QCLI-48`'s task notes. Disposition: none of the 36 are amended or re-trailered — this task's scope is a verification rule plus a sweep, not a history rewrite — and the sweep result itself, re-runnable, stands as the durable record of the gap. `reference/wave-loop.md` section i now states the placement rule, names `git interpret-trailers --parse` as the verification, and shows a worked correct/incorrect pair (`342e76d` / `7efc1a4`). See `QCLI-48` for the full sweep and reasoning.

**Commit rule added (`0.9.1-qcli.5`, `QCLI-49`, 2026-08-07):** `reference/wave-loop.md` sections d and i left one question unanswered — with `backlog/config.yml` setting `auto_commit: false`, does the orchestrator commit its own dispatch-marking task-file writes, given each worker independently commits its *own* copy of the same task file inside its worktree? Doc-11 wave 1 hit the gap directly: `error: cannot rebase: You have unstaged changes`, from uncommitted `wave-1`/`in-review` label edits sitting on the orchestrator's own checkout while the merge queue tried to rebase members onto it. Doc-11 waves 2 and 3 then exercised the "commit it" answer twice, cleanly (`fe92535`, `3633bc1`): the dispatch-marking pass is committed on `<default>` immediately, one `Refs: QCLI-<N>` trailer per task marked in that pass (`QCLI-47`'s hybrid rule, extended to multi-task passes the same way the docs-sync commit already was), and every just-acquired worktree is re-pinned (`git reset --hard`) onto that commit before any worker is dispatched — safe only there, because no worker commit exists yet to lose. That re-pin is what makes a worker's later commit to its own task file (plan/notes) share the dispatch-marking edit as a common ancestor with `<default>`, so the merge-queue rebase in (g) never conflicts on the label lines. A second, sharper root cause surfaced mid-wave-2: an `--add-label in-review` edit run on `dev` for a task whose branch was still unmerged risked the identical conflict, but with no empty worktree left to re-pin onto — resolved by never committing that class of mid-wave task-file edit on `<default>` at all, verifying the dirty diff is label/`updated_date`-only, discarding it before that branch's rebase, and reconstructing the label at settlement instead. `reference/wave-loop.md` sections d, f, g, and i now state both rules explicitly, with runnable commands and the worked commit SHAs above, and (g) carries the resulting precondition: the orchestrator's own `<default>` checkout must be clean before the merge-queue walk starts and stays clean across it, which holds by construction under these two rules rather than by discipline. See `QCLI-49` for the full reasoning.

**Stage-state table reconciled (`0.9.1-qcli.6`, `QCLI-51`, 2026-08-08):** the Campaign stage → Backlog state table listed `in-review` and `merge-pending` as ordinary label rows, but (f) — as amended by `QCLI-49` — applies and then deliberately discards both uncommitted before the affected branch's rebase in (g), reconstructing the real label set only at settlement (i); neither label is therefore ever observable in a committed task file, and R2's crash-recovery cross-check could be read as directing a session to check leftover state against a state that cannot appear. Surfaced by `QCLI-49`'s own worker as an out-of-scope discovery during doc-11 wave 3, correctly left alone there as pre-existing and unrelated to that task's commit-policy scope; re-verified at doc-12 init against `b2ad797` rather than taken on doc-11's word, which corrected two points in the inherited framing rather than confirming it as-is: an add instruction for `merge-pending` does exist (`wave-loop.md`'s (f) step 1, pre-fix) — the defect was a missing point of action, not a missing instruction — and `in-review` sits in the identical position under (f) and is equally affected, not `merge-pending` alone. The owner ruling obtained at that init was broader than any of three offered narrower fixes — treating `merge-pending` as vestigial and dropping the row, giving it a point of action in (g) at the cost of a new write-then-discard ceremony there, or leaving the disposition for the worker to derive — none of which were taken. The disposition: the stage-state table gains a durability column marking `in-review` and `merge-pending` as working-tree-only, with the shared reasoning and each label's distinct point of action stated inline; `reference/wave-loop.md` section (f) now names `merge-pending`'s point of action directly — immediately on the reviewer's `approve` verdict, before (g) — reusing the same run-then-discard mechanism `in-review` already used rather than adding a new one in (g), so section (g)'s merge walk still never mentions either label; and SKILL.md's R2 now states which durable signals — the orchestrator's own `<default>` working-tree cleanliness, the worktree's own commit log, and `gh pr list` — actually classify a leftover branch's review substage, since the two labels themselves cannot. A full `in-review`/`merge-pending` occurrence sweep across SKILL.md and `reference/wave-loop.md` (`QCLI-51`'s task notes) confirmed no remaining passage contradicts this framing. `merge-pending`'s own point-of-action edit has not yet been separately exercised in a recorded wave, unlike `in-review`'s, which doc-11 wave 2's evidence (`QCLI-49`, above) already covers — stated as a gap in (f) rather than overstated as verified. No change to `QCLI-49`'s rule that mid-wave task-file label edits are never committed on `<default>` while the branch is unmerged; this task extends where that rule applies (a second edit under the same rule) without altering the rule itself. See `QCLI-51` for the full reasoning.

**Superseded evidence note (version remains `0.9.1-qcli.9`, `QCLI-55`, 2026-08-13):** The preceding `QCLI-51` paragraph is a dated record of what that task had verified, so its statement that `merge-pending` had not yet been separately exercised remains intact rather than being silently corrected or re-tensed. It ceased to describe current evidence when doc-13 wave 1 exercised the step-2 transition and merged as `d652126`: the wave log records `in-review` at review dispatch, its transition to `merge-pending` on `approve`, both edits left uncommitted and discarded before rebase, and the real labels reconstructed at settlement. The merged QCLI-52 task frontmatter at `d652126` contains only `campaign`, `cluster:skill-docs`, and `wave-1`, and an exact committed-frontmatter search at that revision finds neither review-adjacent label anywhere. `reference/wave-loop.md` section (f)'s operative Evidence paragraph is therefore corrected in place to cite this direct evidence. It also records the necessary limit: doc-13 wave 1 was size 1, so the old per-task discard deadline and (g)'s clean-before-the-walk precondition collapsed onto the same instant and the run supplies no evidence toward `QCLI-53`. The version stays `0.9.1-qcli.9` because this changes only the evidentiary account and preserves all stage transitions, discard timing, recovery behavior, and orchestration requirements; that follows the existing qcli.7 fix-pass precedent for evidence-only corrections rather than assigning a behavioral version bump.

**Stage-state legibility sweep finished (`0.9.1-qcli.7`, `QCLI-52`, 2026-08-08):** `QCLI-51`'s own AC#5 sweep grepped only `in-review`/`merge-pending` occurrences, a method that could not catch two passages describing the same defect class in different words — both surfaced by doc-12 wave 1's integration review. (1) The Conventions table's "Stage state" row read "Status carries the coarse state, labels carry the sub-stage — see the state table below," sitting roughly 15 lines upstream of the table `QCLI-51` corrected and asserting the pre-`QCLI-51` framing unqualified before a cold reader ever reached the correction — not a contradiction (it did point at the table), but the one passage left asserting that framing without qualification. (2) R2 step 5 enumerated three durable review-substage signals (the orchestrator's own `<default>` working-tree dirtiness, the worktree's own `git log`, `gh pr list`) as if that list were exhaustive, omitting the campaign doc's own in-flight table — `reference/templates.md`'s `Stage reached` column, populated per `reference/wave-loop.md`'s per-task stage numbers and, per `reference/wave-loop.md` section (i)'s scope note, "always committed immediately," unlike (f)'s label edits.

Disposition: the Stage-state row now states the split inline — the committed record distinguishes four of the six stages, not all six, with the other two named and cited to `QCLI-51` — so the qualification is visible before the reader reaches the table, not only inside it. R2 step 5 now names the in-flight table as a fourth signal, but the addition is narrower than doc-12 wave 1's own framing offered ("arguably stronger evidence than any of the three currently listed"), and narrower still than this entry first stated. Verifying the signal's real behavior requires enumerating every in-flight-pointer-recording commit in this repo's history, not just the two nearest to hand: `git log dev --format='%h %s' | grep -i "in-flight pointer"` finds **five** — `82fca71` (doc-13 wave-1), `68ce681` (doc-12 wave-1), `61d48af` (doc-11 wave-2), `0b63077` (doc-10 wave-2, recovered), `146956d` (doc-10 wave-1) — not the two this entry originally checked. Four record stage 1 (dispatch); the fifth, `0b63077`, recorded `6 — under review` for a branch a crashed session had only pushed to stage 5, and doc-10's own settled campaign doc later corrects that same row to its true stage (5, pushed) once R2 reconciled it. That is the one later-stage record this repo's history actually carries, and it over-reports rather than confirms — so the added text does not treat a bare numeral as proof either way: it keys the rule on the row's *stated annotation*, requires corroboration against the first three signals before crediting a claimed post-dispatch stage, and states explicitly that both directions of error are real — a stale entry can under-report (nothing mandates a further recording pass at every later transition) and, per `0b63077`, a recorded entry can equally over-report. R2 step 4 is also corrected in this pass: "`In Progress` + `wave-N` means implementation may be mid-flight or done-but-unreviewed" did not name the third, post-`QCLI-51` possibility — already reviewed-and-approved — since neither review-adjacent label ever commits; this is the same defect class the rest of this entry closes, one line above the step 5 edit, included as a deliberate extension of scope rather than a silent drive-by. Verified directly for this correction: all five commits' diffs (the stage numbers and `0b63077`'s row quoted above) and the doc-10 settled campaign doc's inline correction text. Not independently re-verified: whether a differently-worded commit message ever recorded an in-flight pointer without the literal phrase "in-flight pointer" — the enumeration is keyed on that phrase, which every recording commit found here uses consistently (per `reference/wave-loop.md` and `reference/templates.md`'s own usage), but a variant phrasing would not be caught by this grep. Whether `reference/wave-loop.md` should be tightened to mandate additional recording passes (e.g., at approval) remains a distinct, unresolved question, flagged via `--comment` on `QCLI-52` rather than decided here.

A sweep of `SKILL.md` and `reference/*.md` for prose about where campaign substate is recorded, persists, or is legible — not scoped to the `in-review`/`merge-pending` label strings — found no further passage asserting the pre-`QCLI-51` framing; method and full results are in `QCLI-52`'s task notes. The wording mismatch between `reference/wave-loop.md` sections (f) and (g) over exactly when the mid-review label diff is discarded is a distinct, separately-filed defect (`QCLI-53`) and is left untouched here. No change to `QCLI-49`'s or `QCLI-51`'s merged rules — this task only extends what R2's crash-recovery reader is told to check, consistent with both. See `QCLI-52` for the full reasoning.

**Fix pass (mandatory review, 2026-08-08):** the reviewer returned `request_changes` on the paragraphs above: the "two real in-flight-pointer-recording commits" claim was false (five exist; corrected above), the stage-6-as-durable-proof clause was falsified by `0b63077`'s own over-report (corrected above, in R2 step 5), and R2 step 4's leftover-classification prose was incomplete post-`QCLI-51` in the same way step 5 was (corrected above). Re-ran the AC#3 sweep method (task notes) over every passage this fix pass touched — the Stage-state row, R2 steps 4 and 5, and this Provenance entry itself — and found no new instance of the pre-`QCLI-51` framing introduced; the correction narrows and corroborates the prior claims, it does not reintroduce what the sweep was checking for. `reference/wave-loop.md` remains untouched (still `QCLI-53`'s scope), and the version stays `0.9.1-qcli.7` — this is a correction of that entry's own evidentiary support, not a new behavioral change warranting its own bump.

**Discard-timing reconciled (`0.9.1-qcli.8`, `QCLI-53`, 2026-08-08):** `reference/wave-loop.md` section (f) discarded the mid-wave `in-review`/`merge-pending` diff "before that task's branch reaches (g)'s rebase step" — a deadline scoped to that task's own later turn in (g)'s serial merge queue — while (g)'s own precondition paragraph demanded the orchestrator's `<default>` checkout be clean "before this walk starts, and stays clean across every iteration of it" — a wave-wide deadline strictly earlier, for every member queued after the first, than that task's own turn. A per-task-turn deadline also never fires at all for an escalated branch, which never reaches a rebase step. Both wordings were inherited verbatim from `QCLI-49`, written when `in-review` was the only label involved; doc-12 wave 1 ran at wave size 1, which collapses the two deadlines onto the same instant and supplied no case discriminating them. Surfaced during `QCLI-49` work and again by doc-12 wave 1's review, filed as `QCLI-53` with the disposition deliberately left open; acceptance criteria #6–#8 were added by the doc-13 orchestrator before dispatch, on a finding from doc-13 wave 1's integration review, requiring the chosen timing's effect on R2 step 5's crash-recovery signal to be weighed explicitly rather than fixed as a silent regression.

Disposition: (f)'s discard now fires as soon as *that task's own* review reaches a terminal verdict — immediately after the `merge-pending` edit for an `approve` outcome, immediately after the `in-review` edit alone for an `escalate` outcome (the `merge-pending` edit never runs in that case) — rather than deferred to that task's own turn within (g)'s walk. This is a per-task trigger that satisfies (g)'s wave-wide precondition by construction: (g) never begins walking any member until every member's review has settled, so by the time the walk starts every task's own discard has already fired, and nothing further dirties the checkout during any iteration since no further (f) label edit occurs once the walk has begun. Rejected: the fully-tight alternative floated when this task was filed — discarding immediately after *each* edit's own diff-confirmation, collapsing both `in-review`'s and `merge-pending`'s windows to near-zero. `in-review`'s wide window (dispatch through that task's own review settling) is not what caused the (g)-precondition violation — it always resolves before that task's own review settles, which the walk-start gate already requires of every member regardless — so tightening it would have sacrificed R2 step 5's "before-`approve`" pin for no additional safety. Only `merge-pending`'s late-deferred discard was actually unsafe, so only its window is tightened.

Cost, named per AC#6/#7 rather than left implicit: `merge-pending`'s dirty window narrows from "however long until this task's own rebase turn" (which is also what made it unsafe) to the gap between two sequential orchestrator actions — a crash is now unlikely to ever catch it. SKILL.md R2 step 5's first signal is reworded accordingly: it no longer claims both labels pin the crash to before/after `approve` symmetrically — only `in-review`'s appearance still does that reliably; a caught `merge-pending` entry becomes rare rather than routine, and its *absence* must now be read as uninformative, not as "not yet approved." The reviewer is told to corroborate `after-approve` state instead from `gh pr list` (unaffected by this narrowing — an open PR is durable proof of `approve` regardless of whether any dirty diff was ever caught) and the campaign doc's in-flight table; the worktree's own `git log` signal is also named as establishing only that implementation was committed, not that review approved it, a distinction the prior wording left implicit. `reference/wave-loop.md` sections (f) and (g), and SKILL.md's Stage-state block, are reworded to state the same single deadline in the same terms. A sweep of both files for `rebase`, `discard`, and `in-review`/`merge-pending` co-occurrences (command and results in `QCLI-53`'s task notes) found no further passage asserting the old wording as current; two historical Provenance entries above (`QCLI-49`'s and `QCLI-51`'s own dated paragraphs) retain the old phrasing describing what each task changed at the time, left untouched per this document's own append-only Provenance convention rather than rewritten to match. `reference/wave-loop.md`'s Evidence paragraph in (f) and the campaign-doc `Stage reached` material are explicitly out of scope for this task (`QCLI-55` and `QCLI-54` respectively) and are unchanged. See `QCLI-53` for the full sweep commands, results, and reasoning.

**Default-branch push rule added (`0.9.1-qcli.9`, `QCLI-60`, 2026-08-08):** doc-13 wave 2 hit a reproducible loop failure — `git pull --ff-only origin dev` at `reference/wave-loop.md` (g) step 5: "Diverging branches can't be fast-forwarded." Root cause, re-verified directly rather than taken on the task's word: (d) step 4 commits the dispatch-marking pass on `<default>` (wave 2's was `e532f22`, parent `626f369`) but, as written before this task, never pushes it; (d) step 5 then re-pins each worktree onto it, so it reaches `origin` only as content on a worker's branch. `QCLI-53`'s PR (#69) squash-merged that content into `ed3959b`, also parented on `626f369` — confirmed with `gh pr view 69 --json commits`, which lists `e532f22` as the PR's first commit — so `e532f22` and `ed3959b` are siblings, and `git merge-base --is-ancestor e532f22 ed3959b` is false. Local `dev` sat at `e532f22`, orphaned by the squash: no fast-forward. This was a gap in the procedure, not a misexecution — (d) step 4 mandated the commit and said nothing about a push, and the pre-existing crash-visibility parenthetical at (d) step 4 explicitly contemplated an unpushed marking commit as normal ("even if the process dies before the next push"). Two more passages were found asserting the resulting false state as fact, via a sweep of `SKILL.md` and `reference/*.md` for `already an ancestor|ff-only|fast-forward|clean-checkout precondition|diverg|even if the process dies` (results below): (g) step 2 claimed the re-pinned marking commit "is already an ancestor of `origin/<default>`" — true only once the next unrelated push happened to carry it, never by rule; (g) step 5 attributed a failed fast-forward to the clean-checkout precondition, which cannot cause a pure history-topology error and was independently confirmed satisfied throughout (`git status --porcelain` empty).

**Contrasting evidence, also re-verified rather than assumed:** doc-13 wave 1 did *not* hit this failure, but not because it followed a rule — its dispatch-marking commit `fe0e46f` was equally unpushed by itself; `git log dev --oneline | grep fe0e46f` shows it only as an ancestor of `82fca71` (that wave's in-flight-pointer-recording commit, committed on top), and `origin/dev`'s reflog (`git reflog show origin/dev --format='%H %gs'`) shows `82fca71` itself was the commit actually pushed ("update by push"), incidentally carrying `fe0e46f` along before the merge walk ran. No in-flight-pointer commit exists for doc-13 wave 2 (`git log --format='%h %s' | grep -i "record doc-13"` finds only `82fca71` for wave 1) — wave 2 had no equivalent incidental rescue, which is exactly why it, and not wave 1, hit the failure.

**Disposition:** adopt the fix proposed in the task and reject the three recorded alternatives, for the reasons already on record (dropping `--squash` at (g) step 4 reverses a deliberate convention with far wider blast radius; loosening (g) step 5 to `--rebase`/a reset hides the defect and contradicts step 5's own "bug in the loop" stance; not committing the marking pass at all reverts `QCLI-49` and reopens doc-11 wave 1's `cannot rebase: You have unstaged changes`) — research surfaced no better alternative. `reference/wave-loop.md` (d) step 4 now pushes `<default>` immediately after the trailer-parse check, before any worktree is re-pinned or worker dispatched, subject to the skill's existing no-remote convention and a stated non-fast-forward recovery (fetch, rebase — safe pre-dispatch, since no worker commit or re-pin exists yet to disturb — re-confirm the trailer block, retry once, else escalate). **Scope covers both orchestrator-authored `<default>` commit types that land between the wave base and (g)'s walk, not just one:** the in-flight-pointer-recording commit (section i's Scope note) gets the identical push rule for the identical reason, stated inline there rather than left as dispatch marking's unstated twin; settlement and docs-sync commits are unaffected and explicitly named as exempt, because both already run *after* (g)'s walk and (i) step 3 already pushes them. (g) step 2's ancestor claim is now cross-referenced to (d) step 4 as true *by construction*, not merely asserted; (g) step 5's diagnosis now names the real cause class and carries the doc-13 wave-2/wave-1 worked contrast in place of the wrong attribution, while keeping the correct "bug in the loop, not a routine conflict" instruction unchanged. A new recovery procedure at (g) step 5 requires proving no content was lost — enumerating and individually accounting for every local-only commit against `origin/<default>` — before any history-discarding command runs, and `reference/escalation.md`'s error-handling table gains a matching row. (d) step 4's own crash-visibility parenthetical is reconciled rather than left stale: the old "even if the process dies before the next push" framing described a deliberate, tolerated gap that no longer exists under this rule, so it is replaced with the correct post-fix reading — an unpushed marking commit found by R2 is no longer benign; it signals a failed or skipped push that R3 must retry.

**Sweep method and results.** `grep -c -i -E 'already an ancestor|ff-only|fast-forward|clean-checkout precondition|diverg|even if the process dies' SKILL.md reference/*.md`, run against the pre-edit tree (`git show dev:<path>` copies of all four files — not `HEAD:<path>`: once this branch has a commit, `HEAD` *is* the edited tree, so citing `HEAD:` here made the command re-run its own after-state labelled as before; a mandatory-review fix pass caught this) and again against the fully edited worktree. Before: **12 total hits** (`SKILL.md` 4, `reference/wave-loop.md` 7, `reference/escalation.md` 1, `reference/templates.md` 0) — three requiring change (`reference/wave-loop.md`'s then-lines 87, 167, 172: the parenthetical, (g) step 2's ancestor claim, (g) step 5's diagnosis) and nine pre-existing, unrelated hits confirmed by inspection to need no change and stable across every edit below: `SKILL.md:99` (the PR row's `--ff-only` merge fallback), `SKILL.md:159` (`QCLI-53`'s distinct dirty-working-tree "clean-checkout precondition"), `SKILL.md:211`/`213` and `reference/wave-loop.md:276` (each a "**Deliberate divergence**" Provenance-entry header, matching only on "diverg"), `reference/wave-loop.md:96`/`133`/`281` ("diverging from it" in (d) step 5's re-pin explanation, (f)'s own "clean-checkout precondition" text, and the lore-sync section's "fast-forward only" aside), and `reference/escalation.md:38` (the `gh` missing/unauthenticated row's `git merge --ff-only` fallback).

Of the three target passages, only **two** still match the search pattern after either edit below: (g) step 2's corrected text reads "is an ancestor … by construction," which no longer contains "already an ancestor" or any other search term, so it silently drops out of the hit set even though it is one of the three passages this task rewrote. The parenthetical (now (d) step 4's crash-visibility paragraph) and (g) step 5's diagnosis both still match.

After the original edit (commit `007dd56`): **25 total hits** (`SKILL.md` 8, `reference/wave-loop.md` 15, `reference/escalation.md` 2, `reference/templates.md` 0) — correctly decomposed as **9 unrelated + 2 targets still matching + 14 new**, not the "9 unrelated + 3 targets + 13 new" this paragraph originally stated (a mandatory-review fix pass caught this too): the error credited all three targets as still matching instead of two, which understated the new-hit count by one to still land on the correct total.

After this fix pass's own additions (the narrowed recovery in (g) and its `reference/escalation.md` mirror, the SKILL.md R3 push-retry pointer, the (d) step 4 push-failure clause, this correction, and the Fix-pass Provenance paragraph below — all of which necessarily repeat the search vocabulary while describing it): **29 total hits** (`SKILL.md` 12, `reference/wave-loop.md` 15, `reference/escalation.md` 2, `reference/templates.md` 0) — same 9 unrelated, same 2 targets still matching, and 18 new hits from this task's cumulative additions across both the original edit and this fix pass. Separately, by direct reading rather than this grep (neither line contains any of the search terms, so the grep alone would not surface them): `reference/wave-loop.md`'s pre-existing `git push origin <default>` at (i) step 3 (docs-sync push) and `SKILL.md` R5.3's `git push origin <default>` (handover re-arm push) were confirmed to need no change — both already push unconditionally and run after (g), outside either commit type this task governs. Not independently re-verified: whether a differently-worded passage elsewhere in the skill makes an equivalent claim in language these search terms would not catch — the sweep is keyed on the exact phrases the task named plus the mechanism's own vocabulary, consistent with how prior Provenance entries scope their own sweeps (`QCLI-51`, `QCLI-52`).

**Version.** Bumped `0.9.1-qcli.8` → `0.9.1-qcli.9`: this changes required orchestrator behavior (a new mandatory push at two commit points, a new recovery procedure, a new escalation-table row), not merely evidentiary support for prior text, so it warrants its own bump under the same standard `QCLI-49`/`QCLI-51`/`QCLI-52`/`QCLI-53` used (contrast the qcli.7 "Fix pass" entry above, which stayed at `0.9.1-qcli.7` because it only corrected that entry's own evidence, changing no behavior).

Not verified by this task, and left as-is: whether the push rule has ever failed non-fast-forward in practice (no recorded case exists to check against); whether every historical dispatch-marking or in-flight-pointer commit predating this rule is itself an ancestor of `origin/<default>` today (out of scope — this task governs future waves, not a retroactive audit, and no history is rewritten). See `QCLI-60` for the task record, including full reasoning and the raw command output for every claim above.

**Fix pass (mandatory review, 2026-08-08):** the reviewer returned `request_changes` on the `0.9.1-qcli.9` entry above, confirming ACs #1–#5, #8, #9 but failing #6 and holding #7 on it (its text had to change alongside #6's fix). Findings, all addressed:

1. **AC#6, the recovery's "no content was lost" proof was unsound (HIGH).** The original recovery at (g) step 5 whitelisted a local-only commit by *class* — this wave's own unpushed dispatch-marking/in-flight-pointer commit, or a commit predating `QCLI-60` — then asserted unconditionally that any whitelisted commit's content was "already folded into `origin/<default>`'s own history by the squash-merge," discarding it via `git reset --hard`. That rationale holds for the dispatch-marking commit, which reaches the squash by construction because (d) step 5 re-pins the worker's worktree onto it before dispatch — but not for a *mid-wave or R3-recovery* in-flight-pointer-recording commit (the variant section i's trailer table names): it is committed only after every worktree was already re-pinned, so it can never be an ancestor of any worker branch, and a squash-merge that never included it cannot have folded it in — `reset --hard` would silently destroy the campaign doc's in-flight-table update. Nor does "predates `QCLI-60`" say anything about whether the content reached the remote. The check tested class membership, not content presence, which is not what "prove no content was lost" requires.

   Fixed: `reference/wave-loop.md` (g)'s recovery now requires a content-presence check per local-only commit that passes the class check — `git merge-base --is-ancestor <sha> <branch>` against this iteration's own just-squash-merged branch (still checked out, not yet deleted), falling back to a content diff (`git show <sha> --name-only --format=` for its paths, then `git diff <default> origin/<default> -- <paths>`) when that branch ref is already gone. Only a commit passing both the class and content checks is discarded via `reset --hard`; a commit passing the class check but failing the content check is preserved instead — `git reset --hard origin/<default>` then `git cherry-pick <sha>` and `git push origin <default>`, escalating on a cherry-pick conflict or a repeated push failure. `reference/escalation.md`'s matching row is narrowed the same way, closing AC#7.

   **Traced explicitly against the mid-wave in-flight-pointer scenario the finding named:** a wave with members A and B; mid-wave, the orchestrator commits in-flight-pointer commit `P` (touching only the campaign doc) on top of the already-pushed dispatch-marking commit `M`; `P`'s own push fails and the walk proceeds anyway. Member A's branch — re-pinned onto `M` in (d) step 5, before `P` existed — rebases clean onto `origin/<default>` at (g) step 2 (never having seen `P`), merges, and its PR squash-merges to a new commit `S_A` on `origin/<default>`, parented on `M`, that never touched the campaign doc. (g) step 5's `git pull --ff-only` fails: local `<default>` is at `P` (child of `M`), origin is at `S_A` (sibling of `P`). Recovery: local-only = `[P]`. Class check: `P` is this wave's own in-flight-pointer commit whose push failed → passes. Content check: `git merge-base --is-ancestor P <branch-A>` is **false** (`P` postdates A's worktree's re-pin onto `M`, so it was never on A's branch); the diff fallback on the campaign-doc path is **non-empty** (`S_A` never touched it). `P` fails the content check → **not discarded**. Per the corrected step 3: `git reset --hard origin/<default>` (to `S_A`), then `git cherry-pick P` (clean, since `S_A` never touched that path), then `git push origin <default>` republishes `P`'s content as a new SHA on top of `S_A`. The campaign doc's in-flight-table update survives. Under the *original*, unfixed text, step 2 would have unconditionally asserted `P`'s content "already folded into `origin/<default>`'s own history" and discarded it via `reset --hard` — silently destroying it. This confirms the finding and confirms the fix closes it.

2. **Sweep decomposition was off by one (required).** Corrected above, in the "Sweep method and results" paragraph, re-run against the fully edited tree — which now also includes this fix pass's own additions, not just the original edit's.

3. **AC#6's push-retry duty was asserted on R3 without R3 carrying it (required).** `reference/wave-loop.md` (d) step 4's crash-visibility paragraph said an unpushed marking commit means "R3 must retry it," but SKILL.md's R3 never said so — a session reading SKILL.md alone would never learn the duty, unlike the identical lore-sync duty, which carries an explicit "(SKILL.md's R3 carries a pointer to this bullet.)" cross-reference at `reference/wave-loop.md:333`. Fixed: SKILL.md's R3 now states the same duty (retry a missing dispatch-marking/in-flight-pointer push per (d) step 4's recovery, before treating the wave as resumable), and `reference/wave-loop.md` (d) step 4's paragraph now ends with the matching back-reference, following the existing precedent's shape.

4. **The sweep's "before" command was not re-runnable as documented (required).** The sweep paragraph cited `git show HEAD:<path>` for the pre-edit tree; post-commit, `HEAD` *is* the edited tree, so a reader re-running the command on this branch got the *after* numbers labelled *before*. Fixed: cited as `git show dev:<path>` instead — re-run against `dev:`, and the before-numbers (12 total, same per-file split) hold unchanged.

5. **Non-fast-forward is not the only push failure (nit, adopted).** (d) step 4's rejection procedure covered only a non-fast-forward rejection; an auth, network, or unset-`origin/HEAD` push failure was unhandled and is a live path to the same divergence this task exists to close. Adopted a one-clause fix rather than skipping it: any push failure that is not a non-fast-forward rejection now STOPs and escalates immediately, without attempting the fetch/rebase/retry sequence meant for the fast-forward case.

Version stays `0.9.1-qcli.9` — `QCLI-60` has not yet settled, so this fix pass is still part of finishing the one task the prior entry's bump already covers, not a new task earning its own; unlike the qcli.7 fix-pass precedent (which stayed flat because it changed no behavior), finding 1 here is a real behavior change, but it corrects `0.9.1-qcli.9`'s own not-yet-landed behavior rather than adding new behavior on top of already-landed history, so it does not warrant a further bump. `docs/` remains untouched (`git diff --stat dev...HEAD -- docs/` empty, reconfirmed). See `QCLI-60`'s task notes for the reviewer's full findings and this fix pass's disposition of each.
