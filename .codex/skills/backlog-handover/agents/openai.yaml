interface:
  display_name: "Backlog Handover"
  short_description: "Continue multi-session Backlog campaigns safely"
  default_prompt: "Use $backlog-handover to inspect, initialize, restore, or hand over this repository backlog campaign."
