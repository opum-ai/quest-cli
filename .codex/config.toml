model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
plan_mode_reasoning_effort = "medium"
service_tier = "fast"
approval_policy = "on-request"
approvals_reviewer = "auto_review"
default_permissions = "quest-cli-campaign"

[agents]
enabled = true
max_concurrent_threads_per_session = 3
default_subagent_model = "gpt-5.6-terra"
default_subagent_reasoning_effort = "medium"

[permissions.quest-cli-campaign]
description = "Quest CLI-only campaign workspace with environment-file denials and GitHub-only network."
extends = ":workspace"

[permissions.quest-cli-campaign.filesystem.":workspace_roots"]
".git" = "write"
"**/.env" = "deny"
"**/.env.*" = "deny"

[permissions.quest-cli-campaign.network]
enabled = true

[permissions.quest-cli-campaign.network.domains]
"github.com" = "allow"
"*.github.com" = "allow"
"*.githubusercontent.com" = "allow"
