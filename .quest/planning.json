{"milestones":[{"id":"M-1","title":"m-0","status":"open","taskIds":["QCLI-72","QCLI-73","QCLI-74","QCLI-75","QCLI-76","QCLI-77","QCLI-78","QCLI-79","QCLI-80","QCLI-81","QCLI-82","QCLI-83","QCLI-84","QCLI-85","QCLI-86","QCLI-87","QCLI-88","QCLI-89","QCLI-90","QCLI-91","QCLI-92","QCLI-93","QCLI-94","QCLI-95"]}],"decisions":[{"id":"DEC-1","title":"Acceptance criteria and definition-of-done stay advisory at task completion, but completion must report unresolved items","context":"Opened by an inbound report from quest-web (via the orchestrator): a task with one unchecked acceptance criterion completed silently -- exit 0, no warning, nothing in the record. A second, independent incident on 2026-09-13 (another fleet session) closed two tasks with 0 of 3 criteria checked, via the older 1-based --check-ac numbering trap (QCLI-269) whose usage errors went unseen. QCLI-252's own investigation found zero enforcement anywhere in src/ tying acceptanceCriteria/definitionOfDone to the completion transition (only gates/gateEvents block, a structurally separate field), and confirmed command-help.ts and the task-finalization guide never said so plainly.","outcome":"Kept advisory, but stopped being silent: quest task complete now computes an additive unresolvedAtCompletion field (acceptanceCriteria/definitionOfDone unchecked items, index+text) on its JSON result -- present only when something is unresolved, matching this codebase's omit-when-empty convention -- and prints a stderr line prefixed Warning: naming the unresolved items. Exit code and the completed record's own fields are unchanged; opum-cli-e2e's contract check.stderrSilentOnSuccess already tolerates a warning-prefixed line on success, so this does not read as a failure to any known consumer. command-help.ts's task complete summary and the task-finalization guide now state this plainly. Scoped to task complete only (not archive/pause/start/demote), since completion is the one transition where whether the work was actually finished is the live question.","status":"accepted"},{"id":"DEC-2","title":"migration_not_found reclassifies as error_type not_found; sibling migration lifecycle not-found paths move with it","context":"QCLI-257: quest migration backlog status --digest <unknown> --json returned {\"error_type\":\"validation\",\"message\":\"migration_not_found\"} on exit 6, not not_found on exit 3. Root cause: MigrationBacklogService.status() (src/application/migration/backlog-public.ts) throws a plain new Error(\"migration_not_found\") instead of one of the typed domain errors src/cli/main.ts's top-level classifier recognizes; a plain Error whose message is not in its hardcoded not_found list (task_not_found, draft_not_found, milestone_not_found, decision_not_found) falls through to the validation default. rollback() calls status() internally, so it throws and misclassifies the identical message on the identical path -- one fix covers both. apply()'s own not-yet-approved-digest case is a different message, migration_approval_digest_mismatch, raised when a freshly recomputed preview digest does not match the caller-supplied one; that is a genuine input-validation mismatch against current source state; it is not a missing-record lookup, so it does not share this gap and is left classified as validation. Grounding: the shared Opum result contract (Ratify the Quest CLI result contract, QCLI-24/QCLI-69) reserves error_type not_found and exit 3 specifically for a read command's not-found outcome, distinct from validation on exit 6; the component contracts reference document states a read command's not-found outcome must be unambiguous and distinguished from an unrelated hard error.","outcome":"Reclassified: migration_not_found is added to src/cli/main.ts's not_found message list alongside task_not_found/draft_not_found/milestone_not_found/decision_not_found, so quest migration backlog status and quest migration backlog rollback both report error_type: not_found on exit 3 for an unknown or never-previewed digest, matching the name and a caller's reasonable expectation, and matching the ADR's own not-found convention. quest migration backlog apply's migration_approval_digest_mismatch is left as error_type: validation on exit 6 -- it is a digest-mismatch-against-current-source-state validation, not an absent-record lookup, so it does not share the gap this decision addresses. A caller distinguishing the two: branch on error_type (not_found vs validation), not on message text -- message strings are diagnostic detail, not part of the classification contract.","status":"accepted"},{"id":"DEC-3","title":"task view / lore context: projection, enforced bounds, and reported omission are one shared shape across quest and lore (QCLI-276 / LCLI-478)","context":"Two independent gaps turned out to be one problem, surfaced the same night: LCLI-478 (lore-cli) found `lore context`'s maxTokens budget advisory -- a target could exceed it and the result was merely marked truncated, no separate exact-read path existed for proof evidence. QCLI-276 (this repo) found `task view` has no field or note projection and always returns the full record; a probe task's implementationNotes had already passed 1M characters. opum-agent (OPAG-142) ruled the two repos design one agreed shape together before either implements, rather than each inventing a reasonable-but-different answer that a consumer driving both products (mbpm2, concretely) would then have to learn twice.\n\nAgreed between quest-cli and lore-cli sessions, cross-session, 2026-09-14. lore-cli's own side is now written: DEC-2 in lore-cli's own Quest workspace (PR #77), cross-linked here rather than duplicated. DEC-2 marks lore's three still-open specifics -- the hard-budget flag spelling, the exact-read operation's name, and the omission marker's spelling -- as pending design, not declined or deferred; LCLI-478 implementation has not started as of this update.","outcome":"Three SEMANTICS are shared across both repos; the flag spelling, unit, and granularity are NOT forced to match, because the underlying bounds are genuinely different kinds of thing, not the same bound in different units:\n\n1. PROJECTION is additive and opt-in. A consumer names what it wants; absent means everything, so today's default output is byte-for-byte unchanged on both sides. quest's default `task view <id>` stays the full unbounded record -- there is no separately-named \"exact read\" operation the way lore needs one, because projection is purely additive on top of an unchanged default.\n\n2. A SUPPLIED BOUND IS ENFORCED, NOT ADVISORY. If a caller supplies one, the response never silently exceeds it. This is the piece both sides hold hardest: an advisory budget is the same silence-parses-as-optional failure this release (0.7.0) exists to close, in a different costume -- a caller asks for a guarantee and gets a suggestion, with no way to tell which one it received.\n\n3. OMISSION IS ALWAYS REPORTED WHEN PROJECTION IS ACTIVE, NEVER INFERRED FROM A COUNT. Whatever a bounded response drops, it says so explicitly, and the marker is present whenever a projection flag was supplied -- including the zero case (nothing was actually omitted) -- not only when something was. An absent marker is ambiguous between \"nothing omitted\" and \"a caller too old to report it\"; present-and-zero is unambiguous and costs one integer. (The marker is correctly absent when NO projection flag is given at all -- full record, nothing to report.) A consumer must never have to diff against a full fetch to discover a partial one.\n\nUNITS AND GRANULARITY DIFFER BY DESIGN, STATED HERE SO IT READS AS A DECISION RATHER THAN AN INCONSISTENCY: lore bounds by TOKENS across an assembled context pack (a whole-pack budget) because `lore context` assembles an LLM-context pack where token budget is the real constraint and the assembly is lossy by design (selection, ordering, truncation). quest bounds by RECORD COUNT on a single field (a per-field cap: `--max-notes N` on `task view`), because quest is a plain JSON record store with no token-shaped concern and its default is already an exact, unassembled read -- the actual unbounded-growth fields are `implementationNotes` and `comments` arrays, not the record as a whole. These are different KINDS of bound, not the same bound in different units; a consumer working across both products should expect the three semantics above to hold, and should read each tool's own flag list for the specifics, not assume the spelling transfers.\n\nquest-side concrete shape for QCLI-276: `task view` gains `--max-notes N` (and optionally `--since <ISO date>`), additive/opt-in, default unchanged. Whenever any projection flag is supplied, the response carries a `notesOmitted: <count>` sibling field next to `implementationNotes` (0 when nothing was capped, the real count otherwise) -- present whenever projection was requested, never only-when-nonzero. A general `--fields a,b,c` selector for the rest of the record may follow later if a concrete pain point appears; none exists today the way it does for notes.\n\nlore-side concrete shape for LCLI-478: an unbudgeted exact-read operation exists as a SEPARATE named operation, not a flag on the assembling default (structural, since `lore context`'s default is lossy by design, unlike quest's) -- otherwise the same three semantics, its own flag spelling and token-based unit. Full detail in lore-cli's own record, cross-linked once it lands.\n\nNeither QCLI-276 nor LCLI-478 dispatches to an implementer until both sides confirm this record matches their own understanding.","status":"accepted"},{"id":"DEC-4","title":"QCLI-284: version-only stays exit 0 by design; the embedded CI-hint sentence was the thing that lied, not the exit code","context":"lore-cli reported that its CLAUDE.md carried a stale Quest CLI version across two minor releases undetected, because CI's quest agents --check --require-installed --target claude reported exit 0 (state: version-only) the whole time, while the managed block's own generated text read: current instructions exit 0, while missing, drifted, or malformed managed instructions exit 6. That sentence names three failure categories and omits version-only entirely, so a reader concludes CI catches version drift via the exit-6 promise when by design it does not. Two candidate fixes, with different blast radii, were named up front rather than picked blind: (a) the exit code is wrong -- version-only should exit 6 like other drift, meaning every repo running this check in CI has had a gate that reports drift and passes anyway; or (b) the documentation is wrong -- a version-only difference genuinely should not fail a build, meaning this is a wording fix scoped to the sentence. checkQuestBlockOnly (src/application/agents/agent-instructions.ts) already correctly implements QCLI-228s deliberate, tested design: a managed block differing from the installed CLI only in its embedded version number is state version-only and intentionally exits 0 forever, so long as the rest of the content is byte-identical -- QCLI-228 introduced this specifically because gating CI closed on a routine patch/minor version bump alone, with no other content change, was failing builds on drift that was not real. src/application/agents/guides.ts's workspace guide and src/application/command-help.ts's agents entry were both already updated when QCLI-228 landed and correctly describe version-only as a non-failing third state distinct from current/missing/malformed. Only the embedded sentence in questAgentInstructionsFor (the literal text quest agents --update-instructions writes into every consumer's CLAUDE.md/AGENTS.md/GEMINI.md) predates QCLI-228 and was never updated to match.","outcome":"Chose (b): documentation was wrong, not the exit code. The embedded CI-hint sentence in questAgentInstructionsFor (src/application/agents/agent-instructions.ts) now reads: CI should run quest agents --check --require-installed[TARGET]: current instructions, and a version-only difference (only the pinned Quest CLI version number is stale) both exit 0; missing, drifted, or malformed managed instructions exit 6. version-only continues to exit 0 by design (QCLI-228 is not reopened or reversed): a routine patch/minor CLI bump with no other content change in the managed block is not drift, and gating CI on it produces false failures with no actionable fix beyond re-running --update-instructions. This repos own CLAUDE.md managed block was regenerated from the corrected source (quest agents --update-instructions --target claude) so it dogfoods the new sentence and tracker-integrity.yml stays green. test/integration/agents/local-agent-instructions.test.ts gained a test asserting the generated block text explicitly documents the version-only exemption, with a cross-check against checkQuestAgentInstructions's actual behavior for the same input, so a future regression of this exact gap -- text and behavior disagreeing again -- is caught mechanically rather than requiring another externally-filed report. guides.ts and command-help.ts were left unchanged: both already stated the version-only carve-out correctly and needed no correction.","status":"accepted"},{"id":"DEC-5","title":"A checklist replacement is refused exactly when it would clear a checked box; the object form is the escape hatch and no new flag is added","context":"QCLI-313. quest task edit --acceptance-criteria (and --definition-of-done, measured identically) accepts either a JSON array of strings or an array of {index,text,checked} objects. The string form replaces the list wholesale and every checked box silently becomes false: exit 0, empty stderr, kind=task.updated. Re-measured first-hand on a three-criterion probe with positions 1 and 3 checked, and on a two-item definition-of-done with position 1 checked; both cleared.\n\nTHREE INDEPENDENT CALLERS reached for the string form within hours on 2026-09-15, none knowing the object form existed: this repository session (on QCLI-307), opum-agent (on OPAG-148, and it reports having used the string form twice that night), and opum-cli-e2e (on TASK-74, caught before sending). All three were agents that had read quest manifest --json, which describes the flag as {\"value\":\"json-array\"} and nothing more. quest help task edit explains position-vs-index at length and never mentions the object form, which appears in exactly one place in the CLI output: the usage error you get for an object array missing index. Zero actual losses, all three by the same accident -- the criteria involved happened to be unchecked.\n\nOne caller reaching for the destructive path is a caller mistake. Three independent callers reaching for it is the DISCOVERABLE path being the DESTRUCTIVE one. The machine contract advertises the form that destroys state.\n\nThe task left the remedy open between preserve-by-position and refuse. opum-cli-e2e argued for refuse: naming the object shape in the manifest fixes the SHAPE and says nothing about the other form dropping state, so a better manifest makes the trap rarer without making it visible, and rarer-but-silent is the profile that produces a long-lived defect.","outcome":"REFUSE, but only where loss would actually occur, and with no new flag.\n\nTHE RULE. A checklist replacement is refused if and only if some currently-checked item is not checked at the same position in the replacement. If nothing is currently checked, the string form is accepted exactly as today -- which is every existing caller who is not about to lose anything, including every task create call, where there is no prior state at all.\n\nThis makes the refusal and the loss the SAME EVENT rather than two things that usually coincide: no false refusals, and no silent losses. It is statable in one sentence, which matters for something in the machine contract.\n\nWHY NOT PRESERVE-BY-POSITION. It is silent in the other direction and changes the meaning of an operation the contract already documents: a caller who genuinely wanted the reset stops getting it, with no signal. That trades one silent surprise for another, and it never tells the caller what they nearly did -- opum-cli-e2e argument, and it holds.\n\nWHY NOT REFUSE THE STRING FORM OUTRIGHT. Wholesale replace-and-reset is a legitimate operation, and an unconditional refusal breaks every existing string-form caller including the harmless majority. It also breaks task create, where the string form is the right and only sensible spelling.\n\nWHY NO NEW FLAG. The escape hatch already exists and is self-documenting: pass the object form with checked:false to reset deliberately. Adding --force or --replace-checks would be a second way to say something the vocabulary can already express, and the refusal message can name the existing spelling instead of teaching a new one. A refusal that names the object form also closes the discoverability defect at exactly the moment it matters, which is the one moment the caller is definitely reading.\n\nCLASSIFICATION. A record-validation error on exit 6, not usage on exit 2: the command line is well-formed and the conflict is with the current state of the record. This follows DEC-2 reasoning, where a mismatch against current source state stayed validation, and it matches the existing check_operation_conflict, which is already a RecordValidationError raised from the same fold.\n\nSCOPE. Applies to --acceptance-criteria and --definition-of-done identically, since the loss was measured on both. The manifest and help-text criteria of QCLI-313 are still required and are not superseded by this: the refusal makes the trap visible, and the manifest makes the right form discoverable BEFORE the caller hits anything. Both, not either.","status":"accepted"},{"id":"DEC-6","title":"task list names the branch it answered about, and enumerates unseen task ids by set difference only when the listing is empty","context":"QCLI-316, reported by opum-doc and reproduced here. quest task list reads the checked-out ref .quest/ and nothing else, while the rule that sends a session to it -- check nothing is still open before reporting clear -- asks about the REPOSITORY. The two diverge for exactly the tasks most likely to be forgotten, the ones still on an unmerged branch, and the failure direction is the bad one: an empty list reads as THE CHECK PASSING rather than as THE CHECK NOT RUNNING.\n\nThe task named three options and required the choice be made and reasoned rather than left to whichever is easier:\n  (a) FOOTER naming the branch only\n  (b) EXISTENCE NOTICE by set difference, filenames only\n  (c) CROSS-REF READ of status, a merged view\n\nTHE ALLOCATOR PRECEDENT DOES NOT EXTEND FOR FREE, and the reason is not cost. highestSequenceOnOtherRefs already consults every ref for id allocation, which looks like a settled precedent for \"a question about the repository must consult refs\". But its cross-ref operation is max-over-integers on FILENAMES: commutative, order-independent, and incapable of disagreeing with itself. If two refs both carry QCLI-315.json, either answer is the same answer. Status lives INSIDE the record, so (c) needs git show plus a JSON parse per record per ref AND a policy for when two refs carry the same id with different statuses -- QCLI-315 was To Do on its branch and Done on dev within the hour. A union reports a task simultaneously open and closed; a winner-picking rule has silently invented A MERGE POLICY FOR TRACKER STATE, which is a far larger decision than this ticket.","outcome":"OPTION (b), with one refinement the task did not specify and one verification it explicitly demanded.\n\nWHAT SHIPPED. Every task.list envelope carries an additive top-level scope: {branch, otherRefsRead, unseenTaskIds?}. The branch is named on EVERY listing, because \"which object did this answer about\" is a question every result owes an answer to and costs one rev-parse. The cross-ref set difference runs ONLY when the listing came back empty.\n\nWHY EMPTY-ONLY IS A LINE AND NOT A COST DODGE. A non-empty list already tells the reader that records exist and that this is a filtered view. An EMPTY one is the case that reads as the check passing rather than as the check not running, and it is the only case where naming what was not read changes what the reader concludes. The cost saving (about 180ms across 20 refs, measured) is real but is not the argument; if it were, the honest move would be to pay it.\n\nTHE VERIFICATION THE TASK ASKED FOR, AND WHAT IT FOUND. Criterion 2 required confirming the filename-only listing really does answer the safety question rather than taking that from the note. The note recorded ~4,500 raw rows collapsing to ZERO on every one of 20 refs -- proof the notice is QUIET. That is only half a proof, and it is the reassuring half: a notice measured only in its silent state is indistinguishable from one that never fires. Measured the firing case here, which nobody had: from dev at 68b8471 against branch head 4374c69, the difference is exactly QCLI-315 and QCLI-316, the two records dev genuinely could not see, out of 313 and 315 ids. So the notice is both quiet and sensitive, and only now is that measured rather than assumed.\n\nTHE SET DIFFERENCE SPANS tasks/, completed/ AND archive/tasks/. opum-doc flagged it and it is load-bearing: a record legitimately sits in tasks/ on a branch and completed/ on dev, and a tasks/-only comparison reports it as an unseen, still-open PHANTOM. Against 80+ completed records that fires on nearly every ref, which is how a silent notice becomes a noisy one and then an ignored one.\n\nEMPTY IS A RESULT, not an omission. unseenTaskIds: [] says the empty listing IS load-bearing, which is the answer the caller actually wanted. Reporting nothing at all would leave them unable to distinguish \"checked, clean\" from \"did not check\".\n\nWHAT THIS DELIBERATELY DOES NOT DO. It reports EXISTENCE, never status, so it acquires no merge policy. A caller told an id exists elsewhere goes and looks; the CLI never claims to know whether that record is open.\n\nCONTRACT NOTE. scope is additive and sits BEFORE principal, which the shared command contract fixes as the last top-level key -- presence and position are separate constraints there and only presence is obvious. This follows the contractVersion precedent (QCLI-289). A consumer that does not know scope ignores it and reads the same data.\n\nGUIDANCE. quest help task list now states that a result, and an empty result especially, is a claim about the branch rather than the repository, names the scope field, and points at an independent cross-check (gh pr list) -- which is the only thing that caught the original defect, since neither exit code nor output would ever have shown it. The corresponding rule in the opum-sdlc skill lives in opum-fleet and is theirs to update; routed, not changed from here.","status":"accepted"},{"id":"DEC-7","title":"Draft and planning id allocation becomes refs-aware, by a different mechanism from tasks","context":"QCLI-279 made task id allocation refs-aware and deliberately stopped there, recording that nextDraftId and nextPlanningId were left unfixed pending a real reproduction rather than widening the fix without one. QCLI-290 asks whether those two get the same treatment, a different one, or a stated reason to stay as they are. The reproduction arrived on 2026-09-14: two sessions independently minted DEC-3 from branches neither of which carried the other's record.","outcome":"Both get refs-aware allocation, by two different mechanisms, because they are not the same problem wearing two names.\n\nDrafts reuse the existing filename scan against .quest/drafts and .quest/archive/drafts. They are storage-identical to tasks -- one record per file, the id in the file name -- so nothing new was needed beyond making the subdirectory set an explicit parameter rather than a module constant, which also makes the scanned family visible at each call site.\n\nMilestones and decisions do NOT reuse it, and this is the decision worth recording. They share a single .quest/planning.json, so a filename scan over that path matches one file named planning.json, matches no id marker, and returns 0 -- indistinguishable from \"no other ref carries a higher id\". The obvious implementation, which this task's own description proposed, would have returned a wrong answer wearing the shape of a right one. They read blob CONTENT per ref instead, via the revision-pinned read the Git port already exposed. No port change was required.\n\nThis was measured rather than argued. A mutant implementing the obvious fix produces a verdict set byte-identical to a mutant with no fix at all: five of nine tests red, the same five. The wrong mechanism and the absent one are the same observation, which is precisely why the obvious fix would have survived review.\n\nRejected: leaving them as-is. A live collision exists, recovery depended on the actor noticing, and the guarantee was already documented in a way a reader generalises to all four id families.\n\nRejected: a single unified helper over both storage shapes. It would have to branch on storage layout internally, putting the one distinction that matters out of sight of both call sites.\n\nBounds, unchanged from QCLI-279: local refs only, never a fetch; for-each-ref tips only, never a history walk; only a prefix's own family advances its counter; a non-Git .quest directory keeps working-tree-only allocation exactly. Degradation is per-ref rather than per-scan -- an unparseable planning document on one branch is skipped while the others are still read, because a coarser guard falls back to the local-only view, which is the un-fixed behaviour with a passing test.\n\nIds minted before this change are not repaired by upgrading.","status":"accepted"}]}
