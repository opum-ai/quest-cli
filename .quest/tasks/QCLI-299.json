{"id":"QCLI-299","aliases":[],"title":"A publish can report success while the release is not installable: the visibility poll confirms rather than gates","status":"In Progress","description":"Found during the 0.7.0 publish, 2026-09-15. Three defects in scripts/publish-release.mjs, all in the same path, and they compose into one failure mode: a publish can report success while the release is not installable, and the script's own output tells the operator to stop looking.\n\nWHAT HAPPENED. All seven writes returned success. Six packages went public over roughly nine minutes. The seventh, @opum-ai/quest-darwin-x64, returned 409 on a re-attempt: \"Cannot publish over previously staged version 0.7.0\". npm 12 has a staged-publish flow in which a version sits in the registry non-public awaiting a 2FA approval, occupying the semver unique index. So the version was real, reserved, and invisible -- not slow, not failed, not absent. It went public at 13:16:58.797Z, about twelve minutes after the wrapper. For that entire window @opum-ai/quest@0.7.0 was live advertising optionalDependencies[\"@opum-ai/quest-darwin-x64\"] = \"0.7.0\", which did not resolve. Because platform packages are optionalDependencies, a macOS x64 install inside that window SUCCEEDS and leaves no binary. Silent, not loud.\n\nDEFECT 1 -- THE POLL IS A CONFIRMATION, NOT A GATE. The target loop (:207-243) publishes six platforms then the wrapper, with the comment \"Platform packages before the root, so the root never briefly advertises optionalDependencies that do not exist\". That guarantee is about WRITE order. The property needed is VISIBILITY order, and ordering writes cannot produce it. waitForPublished runs at :258, after the whole loop, as the precondition for receipt verification. Reported independently by lore-cli, who hit it first on lore 0.7.0 (a ~121s window), and by opum-cli-e2e; confirmed here by reading this repository's own code.\n\nDEFECT 2 -- THE TIMEOUT MESSAGE TELLS THE OPERATOR TO STOP LOOKING. On timeout the script prints \"npm ALREADY CONFIRMED this publish succeeded above -- this is registry read-after-write lag, not a failed release\" and \"Do NOT run npm unpublish\". That sentence was correct for six packages and wrong for the one that decided whether the release shipped. The unpublish warning must stay -- it is protecting against a genuinely destructive action -- but \"not a failed release\" is an assertion the script has not verified at the moment it prints it. This is the more dangerous of the first two: a gate in the wrong place fails to prevent something, a message like this actively stops the investigation.\n\nDEFECT 3 -- A PUBLISHER-SIDE POLL READS EARLY. Measured by opum-cli-e2e from a non-publishing client: 9-20 seconds between a package's registry time[0.7.0] and its first resolution from a consumer's npm client, across five packages (wrapper +11s, win32-x64 +20s, linux-arm64 +13s, win32-arm64 +12s, darwin-x64 +9s), bounded above by their 10s poll granularity. So even a correctly placed publisher-side gate can report ready while an installer still resolves the old version. Fixing defect 1 alone does not fix this one.\n\nORDERING FACTS, now measured on two releases in two repositories. Write order and registry processing order disagreed in four of six positions here. The package that stalled was written SECOND, ahead of five that completed. \"Publish the wrapper last\" and \"the tail is whatever was written last\" are both dead as models.\n\nWHAT IS NOT KNOWN, stated rather than guessed. Why one of seven staged. All seven used the identical code path, the same granular token and npm 12.0.2; six published outright. npm printed a notice on every invocation that tokens bypassing 2FA are being restricted for direct publishing, which makes a token publish landing in staging plausible -- but that does not explain six-of-seven, and \"it fixed itself\" is not a mechanism. Also unknown: what moved it from staged to public at 13:16:58.797Z. Candidates are an operator approval, the 409 attempt, or staging self-resolving. Determining this is part of the work, because the answer decides whether a staged publish is something the runbook can instruct an operator to clear.\n\nA RELATED ASYMMETRY WORTH FIXING OR AT LEAST DOCUMENTING. The release token could not see the staged version blocking its own release -- npm stage list returned [] for that credential -- and could not see the transition that unblocked it. Both reads that mattered were only available consumer-side. A gate built on the publisher's credential inherits that blindness.","acceptanceCriteria":[{"index":0,"text":"scripts/publish-release.mjs gates the WRAPPER publish on the six platform packages actually resolving, not merely on their writes returning success -- the wrapper is not published until a read confirms all six","checked":true},{"index":1,"text":"The gate reads from a client that is not the publishing one, or tolerates a margin of at least the measured 9-20s publisher-early lag, and the choice is recorded with its reasoning","checked":true},{"index":2,"text":"The timeout path no longer asserts \"not a failed release\" unless it has verified that. It must distinguish, by reading the registry: version public / version staged (409 on a re-publish) / version absent. The npm unpublish warning stays -- that half is protecting against a destructive action","checked":true},{"index":3,"text":"A staged version is reported as its own outcome with the operator action named. If a staged publish cannot be cleared by the publishing credential -- npm stage list returned [] for the release token on 2026-09-15 -- the script says so and names who can clear it, rather than reporting lag","checked":true},{"index":4,"text":"The 2026-09-15 mechanism question is answered or explicitly closed as undetermined: why one package of seven staged, and what moved it to public at 13:16:58.797Z. A recorded \"could not determine, here is what was ruled out\" is a complete answer; a guess presented as the mechanism is not","checked":true},{"index":5,"text":"Verified by making it fail on purpose: with the gate in place, a platform package that is published-but-not-yet-visible must block the wrapper publish, and a staged one must produce the staged outcome rather than the lag message","checked":true},{"index":6,"text":"docs/runbooks/quest-cli-package-and-release.md carries the staged-publish state and the publisher-blindness asymmetry, so the next release does not re-derive them from a 409","checked":true}],"definitionOfDone":[],"plan":["1. New module scripts/qualification/registry-visibility.mjs: consumer-shaped (unauthenticated, no npmrc) packument reads, a settle margin over the measured 9-20s publisher-early lag, a stage-list read, and a version-state classifier","2. publish-release.mjs: extract the publish sequence into an exported, injectable function so the gate is testable, and gate the WRAPPER publish on all six platform packages resolving consumer-side","3. Rewrite the timeout/failure path: drop the unverified not-a-failed-release assertion, keep the unpublish warning, report public/staged/absent-or-staged per package with the operator action named","4. Add --diagnose-staged: the 409 re-publish probe that distinguishes staged from absent, operator-initiated rather than an automatic surprise write","5. Tests that make it fail on purpose: a published-but-not-visible platform must block the wrapper publish, and a staged one must produce the staged outcome rather than the lag message","6. Runbook: add the publisher-early-lag asymmetry alongside the staged-publish section already there; CHANGELOG entry","7. Record the AC5 answer (owner-approved, half-closed) and the reasoning for every choice the ACs ask to be recorded"],"implementationNotes":["HALF THE UNKNOWN IS ANSWERED: THE STAGED VERSION WAS OPERATOR-APPROVED, NOT SELF-RESOLVING. 2026-09-15, from the repository owner directly.\n\nWHAT WAS ASKED AND WHY. AC5 left two questions open: why one package of seven staged, and what moved it to public at 13:16:58.797Z. The second had three candidates -- an operator approval, my failed 409 re-publish nudging it, or staging self-resolving -- and they imply different fixes. Self-resolving would make staging a transient state the script must detect and wait out. Operator-clearable makes it a documented procedure with a named actor. I asked rather than inferred, because the timing alone (409 at 13:12:32Z, public at 13:16:58Z) is consistent with all three and I have been wrong today about exactly this kind of inference.\n\nANSWER: the owner approved it. So staging is operator-clearable, needs 2FA by design, and the release token cannot do it -- npm stage list returned [] for that credential. QCLI-299 AC5 is now half-closed: the transition mechanism is known, the reason one of seven staged is still not.\n\nWHAT THIS CHANGES FOR THE FIX. The script must report a staged version as its own outcome and name who can clear it -- not wait on it, and not call it lag. A wait loop would have burned its full window on a state that was never going to resolve without a human. That is the difference the question bought, and it is why AC5 asked for an answer rather than a best guess.\n\nRECORDED IN THE RUNBOOK, not only here: docs/runbooks/quest-cli-package-and-release.md now carries the staged state, the 409 that identifies it, the two properties that make it hard to diagnose (the publishing token cannot see it; time.modified does not distinguish staged from never-written), and the fact that an approved staged tarball goes public UNMODIFIED -- shasum, integrity, file count and unpacked size all identical to the pre-publication receipt figures, so candidate digests survive an approval and a mismatch after one would be a real finding rather than something to explain away.","IMPLEMENTED. The gate now sits between the platform packages and the wrapper, and reads as a consumer rather than as the publisher.\n\nWHAT CHANGED. New module scripts/qualification/registry-visibility.mjs (plus its .d.mts, since tsconfig has allowJs:false and the sibling scripts already carry declarations). scripts/publish-release.mjs gained three exported, fully injectable functions -- publishPlatformsThenWrapper, describeUnresolvedPackages, diagnoseStaged -- because a gate nobody can make fail on purpose is not a gate anyone has tested. bun run check: 503 tests pass, typecheck/lint/format clean (the one lint warning is pre-existing in test/qcli122-fourth-pass.test.ts, untouched here). lore check: 63 files, 0 errors, 0 warnings.\n\nAC2, THE CHOICE AND ITS REASONING. Both arms, not either: the read goes over plain HTTPS to registry.npmjs.org with NO credential and NO npmrc -- a different client from the publishing one -- AND a 30s settle margin runs after the last package first resolves, with a re-read afterwards. 30s = opum-cli-e2e 20s worst case plus their 10s poll granularity, because that granularity bounds the measurement FROM ABOVE: the real lag for that package was somewhere in (10s, 20s], so 20s is a floor on the worst case observed, not a ceiling on what is possible. Deliberately does NOT bust the CDN cache -- a consumer hits the same cached document, so reading past it would measure something no installer experiences.\n\nAC3, WHERE THE IMPLEMENTATION DEVIATES FROM THE LITERAL WORDING, stated rather than smoothed over. The unverified sentence is gone and the npm unpublish warning stays. The three-way distinction is there, but only two thirds of it is reachable BY READING: the public packument separates public from not-public, and npm stage list separates staged from absent only when the credential can see the stage -- which on 2026-09-15 it could not, returning [] for the token whose own stage was refusing its PUT. So a read that cannot see a stage reports absent-or-staged, named as undetermined rather than collapsed into absent, and the 409 probe that does distinguish them lives behind --diagnose-staged because it costs a WRITE. A poll that silently re-publishes is not something to hide inside a wait loop.\n\nAC5, THE HALF THAT WAS STILL OPEN. Undetermined, with what is ruled out recorded rather than a guess dressed as a mechanism:\n- Nothing in this repository ever asks for a stage. Both publish paths (release.yml per-target loop, publish-release.mjs) invoke npm publish --access public; neither calls npm stage publish. So the stage was created registry-side.\n- npm own documentation says this should not happen. npm help stage and the v12 docs describe staging as an explicit npm stage publish workflow, and GitHub changelog dates the removal of direct publishing for 2FA-bypass granular tokens to AROUND JANUARY 2027. What happened on 2026-09-15 was neither -- an ordinary npm publish with a granular token landing in a stage, four months before the documented enforcement.\n- The most plausible remaining mechanism is a per-package 2FA requirement, and it is not observable from here: npm access set mfa=none|publish|automation is settable, nothing reads it back, and npm access get status returns only public/private (measured: public for all seven).\n- If it recurs, the measurement that settles it is the package settings page on npmjs.com read by the owner, which is the one place the release credential cannot reach.\nThe other half was already answered and stays answered: the owner approved the stage, so staging is operator-clearable rather than self-resolving, which is why the script reports it as an outcome instead of waiting on it.\n\nSCOPE HELD, NOT WIDENED. The CI release workflow has the identical ungated wrapper publish, in a file AC1 does not name. Filed as QCLI-300 with the reason it is not urgent -- trusted publishing is still broken (QCLI-271), so releases go out through the local script that now has the gate -- and the reason it will bite anyway: restoring CI publishing is what makes it live, and that is exactly when nobody will be looking at it."],"comments":[],"labels":["release","packaging"],"documentation":[],"dependencies":["QCLI-289"],"modifiedFiles":["docs/runbooks/quest-cli-package-and-release.md"],"createdAt":"2026-09-15T13:52:46.271Z","updatedAt":"2026-09-15T14:22:08.837Z","blockers":[],"gates":[],"gateEvents":[]}
