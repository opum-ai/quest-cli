{"id":"QCLI-353","aliases":[],"title":"task create swallows a flag-shaped positional title into invalid-arguments with no offending token named","status":"To Do","description":"Reported by opum-agent 2026-09-18: 'quest task create --title \"...\"' fails with only 'task create received invalid arguments.', never naming --title as unrecognized. Root cause verified in src/cli/main.ts: task create's title is positional (rest[0], line 2779-2780), not a --title flag. Because rest[0] is captured unconditionally, the literal string '--title' itself becomes the title text, and the value that follows it ('...') is then parsed by flags() as its own argument -- which doesn't start with '--', so flags() returns undefined outright (main.ts:401) rather than reporting an unrecognized flag. usageFailure (main.ts:484-502) can only name a flag when parsed is defined; when parsed is undefined it has nothing to name and falls back to the generic fallback string, exactly the residual gap its own comment documents ('an unparseable argument list ... still gets fallback, since there is no flag to name', main.ts:470-473). So this is the known QCLI-270/QCLI-344 residual gap, hit by a specific and common trigger: a user supplying a flag that does not exist for a command whose first argument is positional.","acceptanceCriteria":[{"index":0,"text":"quest task create (and any other command with a positional-plus-flags shape) rejects a first positional argument that looks like a flag (starts with --) with a message naming that token, instead of silently treating it as the positional value","checked":false},{"index":1,"text":"quest task create --title \"x\" (or any nonexistent --flag before a positional command) no longer returns the bare fallback usage sentence with no token named","checked":false},{"index":2,"text":"the fix is verified against the exact repro: quest task create --title \"some title\" --json","checked":false}],"definitionOfDone":[],"plan":[],"implementationNotes":[],"comments":[],"labels":["cli-ux","error-messages"],"documentation":[],"dependencies":[],"createdAt":"2026-09-19T02:45:14.484Z","updatedAt":"2026-09-19T02:45:14.484Z","blockers":[],"gates":[],"gateEvents":[]}
