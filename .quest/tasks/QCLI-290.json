{"id":"QCLI-290","aliases":[],"title":"nextDraftId and nextPlanningId (milestone/decision ids) allocate from the working tree only, same collision shape QCLI-279 fixed for tasks -- now reproduced live on a decision id","status":"To Do","description":"QCLI-279 fixed refs-aware allocation for ONE member of a family of three id allocators in src/cli/main.ts; the other two still allocate from the current working tree's .quest/ contents alone, with the exact same collision shape.\n\nRoot cause, unchanged from QCLI-279's own framing: an id allocator's notion of \"next\" is only as complete as the tree in front of it. .quest/ is committed per-branch like any other tracked directory, so any repo with an in-flight branch (or a detached HEAD behind a moved branch) can mint a duplicate id just by allocating from a different ref. QCLI-279 fixed this for task ids specifically -- nextTaskId (main.ts) now takes the max of the working-tree scan and a new highestSequenceOnOtherRefs helper (GitPort.listRefs, a for-each-ref listing across local heads + remote-tracking refs, not a history walk) that scans .quest/tasks, .quest/completed and .quest/archive/tasks on every other ref. That fix was deliberately scoped to tasks only, on the evidence available at the time -- QCLI-279's own notes explicitly left nextDraftId/nextPlanningId unfixed pending a real reproduction, rather than silently widening the fix without one.\n\nThat reproduction now exists. Live, 2026-09-14, during the OPAG-142 0.7.0 campaign: an implementer subagent building QCLI-284 ran `quest decision create` from a branch cut from origin/dev before this session's own `quest decision create` (recording DEC-3) had landed there. Both allocated the id DEC-3 independently -- the coordinating session's DEC-3 landed on dev first (PR #79, commit 9a57c4f); the implementer's own DEC-3 collided the moment its branch fast-forwarded onto the new dev tip. Recovered cleanly (discarded the stale local .quest/planning.json diff, fast-forwarded, re-ran quest decision create, got DEC-4 correctly) -- no data lost, but the collision was real and would not have been caught by a less careful actor.\n\nTwo allocators share the unfixed shape, both in src/cli/main.ts:\n- nextDraftId (~line 793): scans tasks.listDrafts(true) off the working tree only, mints D-<n+1>.\n- nextPlanningId (~line 981): scans planning.listMilestones(true)/listDecisions() off the working tree only, mints M-<n+1> or DEC-<n+1> -- this is the one that just collided.\n\nQCLI-279's highestSequenceOnOtherRefs helper and GitPort.listRefs already exist and are refs-agnostic about what they scan (they take a root and a marker/path set); extending nextDraftId/nextPlanningId to use the same mechanism is very likely a matter of pointing them at the right .quest/ subpaths (drafts' storage location; planning.json's milestone/decision id namespaces) rather than inventing anything new -- confirm this by reading the actual QCLI-279 implementation before assuming it drops in unchanged.","priority":"medium","type":"bug","acceptanceCriteria":[{"index":0,"text":"A decision is made and recorded on whether nextDraftId and nextPlanningId get the same refs-aware fix as nextTaskId (QCLI-279), a different fix, or an explicit reason to leave them as-is -- given the live DEC-3 collision reproduction, leaving them unfixed needs a real justification, not silence","checked":false},{"index":1,"text":"If refs-aware allocation is chosen, it is implemented for draft ids and for both milestone and decision ids (nextPlanningId covers both via the same prefix parameter), demonstrated with a reproduction of the collision this task documents (or one deliberately constructed the way QCLI-279's own test suite did) failing pre-fix and passing post-fix","checked":false},{"index":2,"text":"The chosen behavior is documented in the relevant quest instructions guide (task-creation already carries QCLI-279's refs-aware guarantee for tasks; extend or cross-reference it) so an agent creating a draft, milestone, or decision understands what guarantee it does or does not have","checked":false},{"index":3,"text":"bun run check exits 0 with the new/updated tests included","checked":false}],"definitionOfDone":[],"plan":[],"implementationNotes":[],"comments":[],"labels":["quest-core","reliability","cli"],"documentation":[],"dependencies":["QCLI-279"],"createdAt":"2026-09-14T22:54:33.352Z","updatedAt":"2026-09-14T22:54:33.352Z","blockers":[],"gates":[],"gateEvents":[]}
