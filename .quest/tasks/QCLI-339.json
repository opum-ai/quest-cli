{"id":"QCLI-339","aliases":[],"title":"quest overview counts only .quest/tasks/, so task complete removes a record from the counts instead of moving them","status":"In Progress","description":"Ruled by opum-agent 2026-09-18 (design ruling, implementation spelling left to this repo). Original measurement by lore-cli, relayed: after completing LCLI-540 the overview Done count did not move, 418 before and 418 after, while .quest/tasks/ held 450 and .quest/completed/ held 88.\n\nREPRODUCED FIRST-HAND, and the mechanism is sharper than the relayed framing. In this repo, overview reports total 238, which is exactly the .quest/tasks/ file count, while .quest/completed/ holds 98 (all Done) and .quest/archive/tasks/ holds 1 (Done). Cause: ProjectManager.overview in src/application/planning/planning.ts counts taskSnapshot.tasks, and LocalTaskRepository.readAll filters that array to location === \"tasks\". The snapshot already carries taskRecords with a location discriminator covering all three locations, so no new I/O is needed.\n\nTHE FINDING THE RELAY DOES NOT CONTAIN, measured in a disposable workspace rather than against this tracker: there are two supported routes to the terminal status and they move overview in OPPOSITE directions.\n  - task complete (relocates to completed/): before, In Progress 1 / To Do 1 / total 2. After a successful exit-0 complete: To Do 1 / total 1. No Done bucket appears at all. The record leaves the counted set and total DECREASES.\n  - task edit --status Done from In Progress (does NOT relocate): exit 0, record stays in tasks/, Done 1 / total 1.\nSo overview Done is populated exclusively by the route that bypasses relocation. In a freshly initialized workspace the Done bucket can only ever hold records completed the way the task-finalization guide tells you NOT to. The more correct the workflow, the more wrong the overview. In this repository the 201 Done-in-tasks records are Backlog migration artifacts (no history, completedAt null), which is why a migrated repo sees a large stationary Done number and a clean one sees zero.\n\nNot \"Done means Done-but-not-yet-archived\" as the ruling phrases it: that phrasing holds only for repos carrying migration artifacts.\n\nThe ruling: overview must not report a count whose object is narrower than the object a reader takes it for. Option (a) count across both directories, or (b) keep scope and label it. A third option is argued in the plan and is a synthesis rather than a rejection of either.","acceptanceCriteria":[{"index":0,"text":"In a freshly initialized workspace, quest task complete on a task makes the terminal-status count go UP by one and leaves total unchanged; verified by running overview before and after and by counting each directory separately, naming which directory each number came from","checked":true},{"index":1,"text":"quest overview names the object each task count is about, in both --plain and --json, so .quest/tasks/ and the retention locations are visibly two populations rather than one unlabelled number","checked":true},{"index":2,"text":"The two routes to the terminal status (task complete, and task edit --status to a terminal status) move the overview counts consistently with each other; a probe exercising both produces the same totals","checked":true},{"index":3,"text":"Counts cover .quest/archive/tasks/ as well as .quest/completed/, following DEC-6, which already ratified that a tasks/-only comparison reports phantoms and that the set difference must span all three locations","checked":true},{"index":4,"text":"Any JSON addition sits inside data, leaving contractVersion at index 1 and principal as the last top-level key; asserted against the built binary rather than read off the source","checked":true},{"index":5,"text":"quest search is NOT used to cross-check any number, per the caution on release-shaped skew; every figure in the final summary names its source directory","checked":true}],"definitionOfDone":[],"plan":["1. Reproduce first-hand in a disposable workspace rather than resting on the relayed lore-cli measurement; establish whether the ruling premise holds","2. Count from ProjectManager.overview using taskSnapshot.taskRecords, which already carries all three locations with a discriminator, instead of the narrowed tasks array","3. Add byLocation naming how the total divides, every known location present including zeroes (DEC-6: empty is a result, not an omission)","4. Keep the legacy-reader fallback honest: omit byLocation when the reader cannot report locations rather than claiming one","5. Prove the suite is localised by mutating the implementation and predicting which subset reddens","6. Cross-check every figure against directory counts, never quest search"],"implementationNotes":["CHOSE A SYNTHESIS OF BOTH RULED OPTIONS, not one of them. Option (a) alone (count across locations) fixes the vanishing write but leaves total silently redefined with nothing naming the narrower populations; option (b) alone (label the current scope) leaves the terminal bucket structurally near-empty in any clean workspace, because in a fresh workspace it can only ever hold records that reached Done WITHOUT task complete. Shipped both halves: counts span every retention location AND byLocation names how the total divides. This is not a third design; it is (a) with (b) attached so the narrower object stays visible.","THE THREE-LOCATION SCOPE WAS NOT A JUDGEMENT CALL. DEC-6 already ratified it for the task list cross-ref notice: the set difference spans tasks/, completed/ AND archive/tasks/, because a tasks/-only comparison reports records that legitimately sit elsewhere as phantoms. The ruling named two directories; this repo has three, and .quest/archive/tasks/ held one Done record that option (a) as literally worded would still have hidden. Followed the ratified precedent rather than routing it back.","CODE COMMENT CORRECTED MID-IMPLEMENTATION. A first draft said byLocation deliberately skips sortedCounts so the canonical lifecycle order survives. True of the JSON and FALSE of --plain: renderHumanPayload alphabetises every map, so --plain prints archive/tasks first regardless. Comment now states the order is a JSON-only property. Caught by reading the rendered output instead of the intent -- the same check-the-method-your-tooling-runs failure this repo profile records against its own CI prose.","MUTATION PROOF that the tests pin the properties rather than the implementation identity. Two mutations, each with the red subset PREDICTED FROM READING THE TEST FILE before running. M1, revert total to the narrow array: predicted tests 1,2,3 red and test 4 (envelope key order) green, because the mutant preserves key order; measured exactly that, 3 fail 1 pass. M2, narrow byStatus only and leave total correct: predicted tests 1,2 red and tests 3,4 green, because test 3 asserts byLocation and total but never byStatus; measured exactly that, 2 fail 2 pass. Restored: 4 pass. A mutation that reddened everything would have shown nothing localised.","COVERAGE GAP THAT LET THIS SHIP, since a check that catches something should be asked why nothing caught it earlier. The pre-existing planning suite passed UNCHANGED against the fixed code. Nothing anywhere asserted WHICH population overview counted -- the one overview test used a reader with no taskRecords, so it exercised only the narrow path and could not have observed the defect. The new integration tests spawn the real CLI against a real workspace and cross-check each number against the directories, which is the shape that can see it."],"comments":[],"labels":["defect","contract"],"documentation":[],"dependencies":[],"createdAt":"2026-09-18T17:12:54.938Z","updatedAt":"2026-09-18T17:19:38.721Z","blockers":[],"gates":[],"gateEvents":[]}
