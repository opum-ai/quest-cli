{"id":"QCLI-377","aliases":[],"title":"source-gates reads README.md and LICENSE, but the push trigger paths omit both, so a push touching only those gets no push-triggered run","status":"To Do","description":"Reported by opum-agent under OPAG-432 AC3 (2026-09-25), from its fleet audit tooling/fleet-sweep/bin/paths-coverage-audit.mjs (opum-ai/opum-agent#1030). The audit asks whether a required job's push paths filter covers every file the job reads. Same class as QCLI-376, whose fix (opum-ai/quest-cli#283, 9871531) did not cover this. opum-agent ran the controls by ref: at 4bd161a (9871531's parent) the audit lists QCLI-376's five files plus README.md; at 9871531 it lists README.md only.\n\nVERIFIED HERE at origin/dev 0b26cf7, by git show, not taken from the relay. source-gates runs scripts/qualification/prepublish.mjs, which always runs the package_contents_and_provenance gate (bun run check:packages, i.e. scripts/check-package-artifacts.mjs). That script:\n(1) README.md, CONTENT gate. It extracts package/README.md from the packed root tarball (line 272) and fails on version-claim marker errors or a version token outside a claim region (lines 286-353).\n(2) LICENSE, PRESENCE gate only, found here and NOT in the audit's output. The root pack contents must be exactly LICENSE, README.md, bin/quest.cjs and package.json (line 88), so deleting or renaming the root LICENSE fails the gate. package.json files lists README.md and LICENSE.\n.github/workflows/prepublication-qualification.yml push paths list neither README.md nor LICENSE. The pull_request trigger is unfiltered, so a PR still gets one source-gates row. What is lost is the push-triggered run on dev, and with it the dev-SHA source-gates row a fast-forward promotion needs before a PR exists (see the CLAUDE.md profile section on source-gates and ABSENT).\n\nNOT MEASURED: whether the audit reads presence-only dependencies such as LICENSE at all. If it does not, it has a second blind spot, worth reporting to opum-agent when this lands.","acceptanceCriteria":[{"index":0,"text":"The push trigger paths in prepublication-qualification.yml include README.md and LICENSE, or the task records a written reason for leaving either out","checked":false},{"index":1,"text":"A re-run of opum-agent paths-coverage-audit.mjs against the fixing ref reports no omission for quest-cli, cited with its ref and output","checked":false},{"index":2,"text":"opum-agent is told whether its audit reads presence-only dependencies such as LICENSE, with the LICENSE case as the test","checked":false}],"definitionOfDone":[],"plan":[],"implementationNotes":[],"comments":[],"labels":["ci","testing"],"documentation":[],"dependencies":[],"createdAt":"2026-09-25T21:10:51.866Z","updatedAt":"2026-09-25T21:10:51.866Z","blockers":[],"gates":[],"gateEvents":[]}
