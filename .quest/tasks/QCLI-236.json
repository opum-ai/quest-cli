{"id":"QCLI-236","aliases":[],"title":"Persisted opt-out for the per-repo quest skill (skill_source=plugin equivalent), mirroring lore's LCLI-443/444","status":"To Do","description":"Directed by opum-agent (OPAG-41 cross-session ruling, 2026-09-05): the second, opt-out half of retiring per-repo Claude Code skill distribution, mirroring lore-cli's LCLI-443/444 exactly. Depends on QCLI-235 (the opum-quest plugin scaffold) for the nudge text to have somewhere to point and for dogfooding.\n\nlore-cli's shipped shape (LCLI-443, corrected by LCLI-444 - read both task records in full before implementing):\n\n- Config: `[agents] skill_source = \"plugin\"` in .lore/config.toml (SKILL_SOURCES = [\"repo\",\"plugin\"], default \"repo\" = today's behavior unchanged). Resolution: file value overlaid on default, invalid value fails with a named validation diagnostic.\n- CLI flag: `lore init --skill-source <repo|plugin>` persists it via a byte-preserving regex splice into the existing TOML (not a full reserialize) - preserves every unrelated comment/byte in the file. There is no per-invocation override flag on `lore agents` itself; only `lore init --skill-source` writes the persisted value.\n- \"Explicit beats config\": `applyAgentsBridge`'s `skillSource` is read from config ONLY for the bare `lore agents` command (`includeCodex: true`); a scoped explicit request (`lore init --claude`/`--agents`) hardcodes `skillSource: \"repo\"` regardless of the persisted value, so an explicit ask always still materializes the file.\n- Orphan-drift model (`planSkill`, straight `===` string equality against the deterministic generated template, BOM/CRLF-normalized before comparing): under `skill_source: \"plugin\"`, a present skill file is ALWAYS reported `orphaned` (drift, exit 6 under --check) UNLESS `--force` is given on a non-check run AND the file is byte-identical to the generated output, in which case it's `removed` instead. A file that differs (hand-edited, or generated by an older CLI version) is NEVER removed, even under --force - it stays `orphaned` permanently until deleted by hand or restored to exact generated bytes.\n- CLAUDE.md nudge line varies by skill_source (this is exactly the bug LCLI-444 found and fixed - `buildNudgeBody` originally ignored `skillSource` entirely): `\"- **Skill:** installed from the \\`opum-lore\\` Claude Code plugin, not this repository - how to drive lore.\"` vs the unchanged `\"- **Skill:** \\`.claude/skills/lore/SKILL.md\\` - how to drive lore.\"`\n- No new exit code or JSON envelope shape - `orphaned`/`removed` reuse the existing drift/exit-6 machinery, just two new literal values in what was a closed action union (a breaking addition any exhaustive downstream switch must handle).\n- LCLI-444 also dogfooded: lore-cli's own .lore/config.toml now has `skill_source = \"plugin\"` and its own .claude/skills/lore/SKILL.md is deleted.\n\nquest-cli's starting point is NOT identical to lore's - read this carefully before implementing, do not just port lore's TOML text verbatim:\n\n- Config storage: quest-cli's workspace config is .quest/workspace.toml with a FLAT three-field schema (schemaVersion, name, taskIdPrefix - WorkspaceConfiguration type, src/ports/workspaces.ts:30-34) and NO nested-table concept at all today, unlike lore's .lore/config.toml which already had per-table Zod-loose-object tolerance to extend. This task must DECIDE and BUILD the config storage shape (a new field on workspace.toml, and the schema/serialize/adapter-read machinery to support it), not assume table nesting already exists.\n- Skill-file protection: quest-cli's `updateQuestSkillFile` (agent-instructions.ts:161-170) unconditionally overwrites the skill file on ANY drift or absence today - there is no `protected`/hand-edit-guard concept and no `--force` flag on `quest agents` at all currently. This task adds BOTH the opt-out AND the first-ever hand-edit protection for this file (lore already had `protected` semantics to extend; quest-cli does not).\n- Target-path interaction: quest's skill file path (questSkillPath) does NOT vary by --target today (unlike the AGENTS.md/CLAUDE.md instructions block, which does via agentInstructionPathForTarget, QCLI-227). Decide and record explicitly whether the opt-out applies only under --target claude (since a Claude Code marketplace plugin is a Claude-Code-specific concept) or unconditionally regardless of target.\n- No CHANGELOG.md exists in quest-cli (confirmed) - use docs/reference/quest-cli-release-truth.md's per-release section convention plus this task's own final summary as the durable record, matching every other quest-cli change this session. lore's own precedent: LCLI-444 (the dogfooding/bugfix follow-up) added no separate CHANGELOG bullet of its own, only LCLI-443 (the originating feature) did - if this task similarly splits into a feature part and a same-cycle bugfix/dogfooding part, only the feature part needs the doc-note.","priority":"medium","type":"feature","acceptanceCriteria":[{"index":0,"text":"A design decision is recorded AND implemented for where this config value lives (extending .quest/workspace.toml's schema) and what it's called, including the schema/serialize/adapter-read machinery actually built - not assumed to already support nested tables the way lore's .lore/config.toml did","checked":false},{"index":1,"text":"Under the opt-in setting, quest agents --check/--update-instructions neither writes nor proposes .claude/skills/quest/SKILL.md; a leftover file is reported as drift (exit 6 under --check) naming the file and the remedy; default/unset behavior is verified unchanged byte-for-byte against the existing test suite","checked":false},{"index":2,"text":"A new --force flag on quest agents removes a leftover skill file only when it is byte-identical to quest's own generated questSkillContent; a hand-edited or otherwise-differing file is left in place even under --force - note in the implementation that this is also the first time quest-cli's skill file gains any hand-edit protection at all, since today it has none","checked":false},{"index":3,"text":"An explicit, scoped write request (quest init --agent-instructions, or an equivalent explicit flag) always still materializes the skill file regardless of the persisted opt-out setting - 'explicit beats config', tested directly","checked":false},{"index":4,"text":"A decision is recorded and implemented for whether the opt-out applies only under --target claude or unconditionally, given questSkillPath does not vary by --target today","checked":false},{"index":5,"text":"The CLAUDE.md managed-instructions-block prose (or an equivalent nudge) reflects the opt-out, telling a reader the quest skill comes from the opum-quest plugin rather than this repository, mirroring lore's LCLI-444 fix for the identical bug class (a nudge whose text didn't vary by the new setting)","checked":false},{"index":6,"text":"Tests cover: default unchanged, opt-in (no write/propose + drift report + force-removal-only-on-byte-match + hand-edit-never-removed), and explicit-request-overrides-opt-in","checked":false},{"index":7,"text":"This repository (quest-cli itself) is dogfooded once QCLI-235's plugin scaffold exists: quest-cli's own workspace config is set to the opt-in value and its own .claude/skills/quest/SKILL.md is deleted, mirroring LCLI-444","checked":false}],"definitionOfDone":[],"plan":[],"implementationNotes":[],"comments":[],"labels":["agents","config","fleet-cutover","opum-workflow"],"documentation":[],"dependencies":["QCLI-235"],"references":["lore-cli:LCLI-443","lore-cli:LCLI-444","lore-cli:src/config.ts","lore-cli:src/core/agent-bridge.ts","lore-cli:src/commands/agents.ts","src/application/agents/agent-instructions.ts","src/ports/workspaces.ts"],"createdAt":"2026-09-05T17:47:28.484Z","updatedAt":"2026-09-05T17:47:28.484Z","blockers":[],"gates":[],"gateEvents":[]}
