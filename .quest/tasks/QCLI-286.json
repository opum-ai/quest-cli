{"id":"QCLI-286","aliases":[],"title":"A rolled-forward CHANGELOG version section can silently swallow later fixes -- catch it, not just document it","status":"To Do","description":"Root cause identified by the orchestrator while writing a downstream consumer brief for quest-cli 0.6.2, verified directly against CHANGELOG.md rather than assumed: the published 0.6.2 CHANGELOG section documented only the QCLI-264/QCLI-265 envelope-shape breaks -- the content 0.6.1 would have carried before it failed to publish -- and omitted five real fixes (QCLI-247, QCLI-257, QCLI-261, QCLI-269, QCLI-270) that landed on dev and shipped in the actual published 0.6.2 bytes. QCLI-269 is the most consequential: it is the 1-based/0-based --check-ac fix, the defect that started this entire release cycle, and a consumer reading the CHANGELOG to check whether it was fixed would have concluded it was not. Corrected in a follow-up commit, but the mechanism that produced the gap is the more valuable finding and is what this task tracks.\n\nMechanism: the 0.6.2 section was originally written during 0.6.1's release attempt (0.6.1 was tagged but never published due to a CI/OIDC defect). When the content was carried forward under 0.6.2's version number rather than writing a fresh section, the version number was already \"claimed\" by an existing section -- so the five fixes that landed on dev AFTER that section was written, but BEFORE the 0.6.2 tag was actually cut, never triggered the usual \"write a changelog entry\" prompt, because there was no longer an empty \"## Unreleased\" section pointing at them. They landed invisibly under a pre-existing, already-written version heading.\n\nThis is a process defect, not a one-off typo, and it will recur on any future release that rolls forward a version number this way (which this repository has now done twice: 0.6.0's \"minor not patch\" framing and 0.6.1-to-0.6.2 both involved a version section written before all its content existed).","acceptanceCriteria":[{"index":0,"text":"A concrete mechanism is decided and implemented that catches this class of gap before a release ships -- e.g. a release-gate check comparing every task/PR merged since the prior tag against CHANGELOG.md mentions, or restructuring the release procedure so a version section is only ever finalized at tag time rather than pre-written","checked":false},{"index":1,"text":"The chosen mechanism is documented in docs/runbooks/quest-cli-package-and-release.md at the step where a version section is normally written","checked":false},{"index":2,"text":"If a check is implemented, it runs in CI (or as an explicit pre-tag step in the runbook) rather than existing only as written guidance a human must remember","checked":false}],"definitionOfDone":[],"plan":[],"implementationNotes":["LIVE RECURRENCE, 2026-09-15, found while verifying an unrelated report from opum-fleet -- and the cause is ADJACENT TO, not identical to, the one in the description. Stating the difference rather than collapsing it, because the fix differs.\n\nThe description diagnoses a ROLLED-FORWARD version section: content written under 0.6.1, reused under 0.6.2, so later work had no empty \"## Unreleased\" heading to land under. Today the 0.7.1 section is correct and correctly finalized. The defect is that release prep RENAMED the Unreleased section into \"## 0.7.1\" and left nothing behind it, so CHANGELOG.md on dev currently has NO Unreleased section at all. Different cause, identical consequence, and it is the consequence this task names: nothing points at work that has landed since the tag.\n\nMeasured, not inferred. v0.7.1 peels to 9258018, which is the release-prep commit itself. Since it, three commits touch src/ and none is mentioned anywhere in CHANGELOG.md:\n\n  e9c3c62  feat(QCLI-316)  task list gains a top-level \"scope\" key\n  8f474ef  fix(QCLI-313)   checklist replacement refuses a silent box-clear\n  5544ac3  fix(QCLI-315)   agent-guide prose contradicted the flag it documents\n\nplus 0c3117f (QCLI-307), which rewrote README.md -- a file that ships inside the published package -- and added a pack-time gate for it.\n\nThis is what makes it consumer-visible rather than tidiness. opum-fleet reported today that it cannot detect QCLI-316 \"scope\" from the installed build, and dev declaring 0.7.1 gives it no version to gate on either (see QCLI-296). CHANGELOG.md was the one remaining surface that could have told it \"scope\" exists and is unreleased, and that surface is silent too. Two independent records of pending work, both blank, and the consumer wrote a key-presence workaround instead.\n\nSo AC1s second option -- \"restructuring the release procedure so a version section is only ever finalized at tag time\" -- has a cheaper sibling this instance argues for directly: release prep must LEAVE A FRESH EMPTY \"## Unreleased\" behind when it renames one. That step is missing from the runbook and its absence is what produced this occurrence.","SECOND OCCURRENCE, 2026-09-18 (QCLI-345). This task predicted the mechanism and the mechanism repeated, in the same shape, one release later.\n\nThe 0.8.0 section was frozen and tagged at 9d91fc35 on 2026-09-17 and never published. Fifteen commits then landed on dev. Five touch src/ and are consumer-observable -- QCLI-336 (feat: unresolvedAtCompletion persisted and queryable), QCLI-339 and QCLI-340 (overview count scope), QCLI-342 (overview guide envelope), QCLI-344 (task edit --implementation-notes) -- and none appeared in the CHANGELOG, which carried an empty Unreleased heading directly above the frozen 0.8.0 section.\n\nStructurally identical to the 0.6.1-to-0.6.2 instance: a version section written during a release attempt that did not publish, a version number already claimed by an existing section, and later fixes landing on dev with no unclaimed heading to go under. The only difference is that it was caught before publication this time, by walking git log v0.8.0..dev by hand during release preparation -- precisely the manual step AC1 asks to replace with a check.\n\nNOT CLOSED, deliberately. opum-agent asked for this loop to be closed on the record as a realized instance. Closing it would falsify all three acceptance criteria, which require a mechanism decided, implemented, documented in the release runbook, and running in CI or as an explicit pre-tag step. None of that exists yet; only the evidence that it is needed does.\n\nWhat this occurrence adds for whoever implements it: the detector has a cheap and exact form. git log <last tag>..HEAD -- src/ returns the consumer-observable commit set directly -- 5 of 15 here, no false positives, no judgement call -- and each of those commit subjects already carries its task id. Asserting that every such id appears in the CHANGELOG at or above the current version section would have failed loudly on this tree tonight, and on the 0.6.2 tree."],"comments":[],"labels":["release","documentation"],"documentation":[],"dependencies":[],"createdAt":"2026-09-14T16:58:58.198Z","updatedAt":"2026-09-18T23:30:44.222Z","blockers":[],"gates":[],"gateEvents":[]}
