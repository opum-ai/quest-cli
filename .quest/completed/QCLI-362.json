{"id":"QCLI-362","aliases":[],"title":"The managed agent-instructions block points at itself and never names the guides, search, or the actor flags","status":"Done","description":"Routed by opum-agent from its OPAG-378 primer audit (report: opum-agent origin/dev scratchpad/opag-378-primer-audit-2026-09-23.md), measured against quest 0.9.0 and re-measured here. The template in src/application/agents/agent-instructions.ts is regenerated into every fleet repository. (1) It says `quest instructions --json` returns \"the current versioned protocol\", but that command returns this same block (1,061 content bytes here), so the pointer is circular. (2) It never names `quest instructions --list`, the overview, the four task guides, `quest search`, or `quest help`. (3) It carries the Backlog cutover recipe, which the workspace guide already covers (its \"Coming from Backlog.md\" section) and every fleet repo has already run. (4) It says writes need an actor declaration without naming --actor, --actor-kind or --accountable-human. Consumer sweep before editing: opum-cli-e2e suites 10/20/22 match on the begin/end markers, the \"# Quest agent instructions\" heading and content length. The opum-fleet regenerate-quest-block.mjs parses \"Quest CLI x.y.z\" inside the markers. Local tests pin \"This project uses Quest CLI <v> for tracker operations.\", the CI exit sentence and \"Quest writes require an explicit actor declaration\". All of those must survive.","acceptanceCriteria":[{"index":0,"text":"The block no longer describes `quest instructions --json` as the versioned protocol; it names `quest instructions --list` and the overview, task-creation, task-execution, task-finalization and workspace guides with when to read each","checked":true},{"index":1,"text":"The block names `quest search` for finding existing records before creating one, and `quest help <command>`","checked":true},{"index":2,"text":"The Backlog migration recipe is removed from the block and remains reachable through the workspace guide, verified by reading that guide","checked":true},{"index":3,"text":"The block names the actor flags for both a human and a delegated agent: --actor, --actor-kind human|delegated-agent, --accountable-human","checked":true},{"index":4,"text":"The begin/end markers, the heading, the `Quest CLI <version> for tracker operations.` sentence, the CI exit sentence and the phrase `Quest writes require an explicit actor declaration` are preserved, and the version-only check still reports version-only on a block differing only in its version","checked":true},{"index":5,"text":"bun test and the repository check pass; this repo own CLAUDE.md managed block is regenerated in the same PR and `quest agents --check --target claude` exits 0 against the built checkout","checked":true}],"definitionOfDone":[],"plan":[],"implementationNotes":[],"comments":[],"labels":["agents","docs"],"documentation":[],"dependencies":[],"createdAt":"2026-09-24T04:14:29.091Z","updatedAt":"2026-09-24T04:21:23.322Z","finalSummary":"The managed agent-instructions block now sends a reader to the guides instead of back to itself (PR #239). The change was routed by opum-agent from its OPAG-378 primer audit, and all four findings were re-measured here against quest 0.9.0 first.\n\nWhat changed in src/application/agents/agent-instructions.ts:\n1. Removed the circular \"`quest instructions --json` for the current versioned protocol\". That command returns this same block, 1,061 content bytes here. The block now names each guide and when to read it (overview, task-creation, task-execution, task-finalization, workspace), plus `quest instructions --list`.\n2. Added `quest search \"<query>\" --json`, to find an existing record before creating one, and `quest help <command>`.\n3. Removed the Backlog cutover recipe. It stays reachable through the workspace guide section \"Coming from Backlog.md\", checked by reading that guide.\n4. The actor declaration now names its flags: `--actor <id> --actor-kind human`, or `--actor <id> --actor-kind delegated-agent --accountable-human <id>`.\nThe block grows from 1,061 to 1,578 bytes.\n\nKept verbatim, because a consumer sweep found things matching on them:\n- the begin/end markers, the heading and the first 80 characters (opum-cli-e2e suites 10, 20 and 22);\n- \"Quest CLI <v> for tracker operations.\" (EMBEDDED_VERSION_PATTERN, and opum-fleet regenerate-quest-block.mjs);\n- the CI exit sentence and \"Quest writes require an explicit actor declaration\" (local tests).\nTwo patterns were searched: the block phrases, and callers that invoke instructions. Scope: this repo, plus siblings origin/dev excluding CLAUDE.md, AGENTS.md and .quest. The e2e baselines/ and evidence/ hits are recorded outputs, not assertions.\n\nVerification:\n- The new test fails against the pre-fix template (1 fail, 18 pass) with only the source reverted, and passes with the fix.\n- The existing version-only tests still pass.\n- npm run check: 593 pass, 0 fail. lore check exits 0.\n- This repo CLAUDE.md block was regenerated from the checkout, with only lines between the markers changed, and agents --check --require-installed --target claude reports current.\n- CI on PR #239 head 3d7e441: source-gates 35955061335, Tracker integrity 35955061281, lore check 35955061337, all success.\n\nConsumer effect: every fleet block reports drift (exit 6) until regenerated after release. This is a real content change, not version-only, and the CHANGELOG Unreleased entry says so.\n\nVersions: quest 0.9.0 installed, checkout build at package.json 0.9.0; lore 0.9.0.","blockers":[],"gates":[],"gateEvents":[]}
