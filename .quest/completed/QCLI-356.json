{"id":"QCLI-356","aliases":[],"title":"The required lore check gate pins lore 0.7.0, which cannot detect schema drift at all, and the repo has 7 drifted schemas under 0.8.0","status":"Done","description":"Found 2026-09-19 while running lore check as the definition of done on an unrelated tracker-only change (QCLI-355). Not reported by anyone; the gate reported it about itself.\n\nTHE MEASUREMENT. Same tree, same commit, two lore versions, opposite verdicts:\n\n  lore 0.7.0 (what CI installs)   exit 0    63 files, 0 errors\n  lore 0.8.0 (what workstations run)  exit 6   63 files, 7 errors, all schema-drift on .lore/schemas/*.json\n\nThe seven are epic, story, spec, adr, runbook, reference and attested-computation, each reporting that the committed schema no longer matches what this profile generates.\n\nWHY CI IS GREEN, AND WHY THAT IS WORSE THAN A RED. .github/workflows/lore-check.yml:100 installs @opum-ai/lore@0.7.0. The pin is deliberate and its rationale is sound as written -- it tracks the lore release paired with this package and is hand-bumped when the pairing moves, because deriving it from package.json would red the very PR that cuts a release. But lore 0.7.0 DOES NOT IMPLEMENT SCHEMA-DRIFT DETECTION AT ALL, so the required gate is not merely out of date on this class, it is blind to it.\n\nPROVED IN BOTH DIRECTIONS rather than inferred from the version numbers. I injected a deliberate key into .lore/schemas/adr.schema.json and re-ran both:\n\n  lore 0.7.0 on the DELIBERATELY drifted tree   exit 0, 0 errors    <- cannot see it\n  lore 0.8.0 on the same drifted tree           exit 6, schema-drift reported\n\nand reverted the injection (tree confirmed clean afterwards). The 0.7.0 row is the load-bearing one: a gate that returns 0 errors on a tree someone has visibly corrupted is not passing, it is not looking.\n\nTHIS WAS FORESEEABLE FROM THIS REPOSITORY OWN CHANGELOG, which is the part worth keeping. The 0.9.0 entry, in the QCLI-345 correction, already records that lore dev past v0.7.0 carries \"committed-schema drift detection inside lore check\". So the capability was known here on 2026-09-18 and the gate that would exercise it was never moved to a version that has it. Nothing connected the two facts because they live in different files.\n\nCONSEQUENCE FOR THE REQUIRED-CHECKS RULESET. lore check is one of the three required contexts on main (source-gates, Tracker integrity, lore check). A promotion can therefore go green on main while the docs bundle carries an error class the gate cannot represent. That is the same shape as a check whose clean answer authorises an action without a positive control -- the green means \"0.7.0 found nothing\", and is being read as \"the bundle is clean\".\n\nTHE FIX IS UNBLOCKED, which is the useful half. Because 0.7.0 cannot see schemas at all, regenerating them under 0.8.0 CANNOT turn the pinned CI red. So the ordering constraint one would expect here does not exist, and the two steps are independent:\n  1. lore schema export under 0.8.0, commit the regenerated schemas. Local goes green; CI at 0.7.0 is unaffected because it does not look.\n  2. Bump the workflow pin so the gate actually enforces the class. This is the step that changes what a REQUIRED context on main asserts, so it is the one that needs a decision rather than just an edit.\n\nStep 2 also needs the pairing question answered rather than assumed: published lore is 0.8.0 and published quest is 0.9.0, and CHANGELOG already declares the lockstep broken deliberately (QCLI-328 rule, corrected by QCLI-345). So \"the lore release paired with this package\" no longer names a single number, and the workflow comment instructing a hand-bump to the paired version has no unambiguous answer today. That wording needs to change with the pin.\n\nA THIRD THING TO CORRECT, smaller and in the same file. The workflow DETERMINISM paragraph claims \"a clean run locally is a clean run here for the same tree\". That is now demonstrably false: it holds the TREE constant and lets the TOOL vary, and the tool is exactly what differed here. The sentence should name the lore version as part of what has to match, or it will reassure the next reader out of investigating precisely this.","priority":"High","type":"bug","acceptanceCriteria":[{"index":0,"text":"lore check exits 0 against the workstation lore version rather than only against the pinned one, verified by running both and recording the version each resolved","checked":true},{"index":1,"text":"The lore-check workflow pin names a version whose lore check implements schema-drift detection, demonstrated by a deliberate drift going red in CI and a clean tree going green in CI as two separate runs on two separate commits","checked":true},{"index":2,"text":"The workflow determinism comment no longer claims a clean local run implies a clean CI run for the same tree without naming the lore version as part of what must match","checked":true},{"index":3,"text":"The workflow comment instructing a hand-bump to the paired lore release is reworded to say what to do now that CHANGELOG declares the quest/lore lockstep deliberately broken and the two published numbers differ","checked":true},{"index":4,"text":"Whatever resolves this states which lore version it was measured against, so the next reader can compare it to their own installed binary rather than re-deriving it","checked":true}],"definitionOfDone":[],"plan":["1. Regenerate .lore/schemas under lore 0.8.0 and land it alone; prove lore 0.7.0 stays green on the regenerated tree so the pinned CI cannot red","2. Change the workflow pin to 0.8.0 in a separate change","3. Prove both arms as separate CI measurements: clean tree green under the new pin, deliberate drift RED with schema-drift in the log","4. Correct the workflow determinism comment and the paired-release hand-bump wording"],"implementationNotes":["Step 1 measured before landing. Regenerated .lore/schemas with lore 0.8.0 (lore schema export, exit 0, 7 schemas). Result on the regenerated tree: lore 0.8.0 exit 0 (63 files, 0 errors) and lore 0.7.0 ALSO exit 0 (63 files, 0 errors). So landing the regeneration alone cannot red the pinned CI -- measured on both binaries rather than inferred from 0.7.0 being blind.\n\nTWO CORRECTIONS TO THE ROUTED RULING, both measured here. (1) The relayed figure \"777 insertions and ZERO deletions, purely additive\" does not reproduce in this repository: git diff --numstat reports 2067 insertions and 42 DELETIONS across the 7 schemas, six files carrying 7 deletions each. (2) The deletions are not formatting. adr.schema.json status changes from anyOf[string, null] -- any string, plus null -- to type string with enum [draft, stable, deprecated]. That is a NARROWING: the regenerated schema rejects values the committed one accepted. So the reassurance that 0.7.0 \"is validating a real subset\" does not hold for this field; for status the committed schema was the LOOSER one, and the pinned gate has been accepting adr status values the current profile rejects. That strengthens the case for the bump rather than softening it.\n\nMost likely explanation for the gap, not yet confirmed with opum-cli-e2e: they measured their own bundle, which is a different object with a different profile. Their number may be correct about their repository and simply does not describe this one.","Step 1 landed as dc272c1 (PR #222). Verified on dev after merge: lore 0.8.0 exit 0 and lore 0.7.0 exit 0. CI lore check also passed on the regenerated tree at the pinned 0.7.0, so the cannot-red-the-pinned-CI claim is confirmed at CI level, not only locally. Acceptance criterion 1 is satisfied.\n\nSTEP 2 IS BLOCKED, reported to opum-agent. The routed ruling says to make the workflow READ candidate-pin.json rather than carry a second literal. No such file exists in this repository: find returns nothing and a grep for the name across yml/json/md/ts returns nothing. Not substituting an invented mechanism for a named one.\n\nEstablished while reporting, and it constrains whatever mechanism is chosen: the gate cannot detect its own staleness from inside. lore check runs AT the pinned version and compares committed schemas against what THAT version generates, so once the pin is 0.8.0 and the schemas match, a later lore 0.9.0 is invisible to it. Any real staleness detector must compare the pin against something outside the run, which is presumably what candidate-pin.json is for.\n\nStated fallback absent an answer: bump the literal to 0.8.0 to close the false green on main, and file the staleness detector as its own task rather than improvising one. Step 3 is not started, because its two arms are measurements OF the new pin.","Steps 2 and 3 complete. Pin bumped to 0.8.0 in PR #224 (376940a), with two comment corrections in the same file.\n\nSTEP 3 PROVED IN BOTH DIRECTIONS AS SEPARATE MEASUREMENTS ON SEPARATE COMMITS, as ruled.\n\nCLEAN ARM, PR #224, clean tree under the new pin: lore check pass, source-gates pass, Tracker integrity pass.\n\nDRIFT ARM, PR #225 on a throwaway branch built ON TOP of the pin bump so it ran under the NEW pin, with a bogus property injected into adr.schema.json. The log, not merely the colour:\n\n  error .lore/schemas/adr.schema.json [schema-drift]: the committed schema no longer matches what this profile generates\n  63 files, 1 error, 0 warnings, 5 out-of-bundle links skipped\n  Process completed with exit code 6\n\nExactly one error, naming the schema-drift class and only the file deliberately corrupted, at exit 6. source-gates and Tracker integrity both passed on that same SHA, so the gate moved for its own reason rather than because something general broke -- which is the check that distinguishes a working gate from an always-red one. PR #225 was closed unmerged and its branch deleted; it was an instrument, never a change.\n\nThe staleness limit is split out as QCLI-358 with the argument carried verbatim, per the ruling. candidate-pin.json was withdrawn by the orchestrator as an object-identity error: it is opum-cli-e2e file in opum-cli-e2e repository and does not exist here.\n\nVersions this proof resolved against, recorded per the fleet rule rather than left to inference: workstation lore 0.8.0, CI pinned lore 0.8.0 after the bump and 0.7.0 before it, quest 0.9.0."],"comments":[],"labels":["ci","docs"],"documentation":[],"dependencies":[],"createdAt":"2026-09-19T19:48:13.495Z","updatedAt":"2026-09-19T20:59:22.827Z","finalSummary":"Closed the false green on a required context. The lore check gate was pinned to lore 0.7.0, which has NO schema-drift detection at all, so a context required for promotion to main was reporting success on a class it could not represent.\n\nFound by running lore check as the definition of done on an unrelated tracker-only change; nobody reported it.\n\nDELIVERED IN THREE ORDERED CHANGES, per the orchestrator ruling.\n\n1. dc272c1 (PR #222) regenerated the committed schemas under lore 0.8.0, landed alone. Measured on both binaries before landing rather than inferred: on the regenerated tree lore 0.8.0 exits 0 and lore 0.7.0 also exits 0, so it could not red the pinned CI. CI own lore check confirmed that at the pinned 0.7.0.\n\n2. 376940a (PR #224) bumped the pin to 0.8.0 and corrected two comments in the same file that had gone false. The hand-bump-to-the-paired-release instruction could not be followed, because CHANGELOG declares the quest/lore lockstep deliberately broken and the two published numbers differ; the pin now documents itself as the newest published lore this repository has been MEASURED against, and records the two-step bump order. The determinism paragraph claimed a clean local run implies a clean CI run for the same tree, which is exactly how this failed -- same tree, 0.7.0 exit 0 and 0.8.0 exit 6 -- so the lore version is now named as part of the precondition.\n\n3. Proof in both directions, separate measurements on separate commits. Clean arm PR #224 green. Drift arm PR #225, branched on top of the bump so it ran under the NEW pin, with a bogus property injected into adr.schema.json: exactly one error, schema-drift, naming only the corrupted file, exit 6 -- asserted from the job log rather than from the colour, because an unrelated red looks identical. source-gates and Tracker integrity passed on that same SHA, so the gate moved for its own reason. PR #225 closed unmerged, branch deleted; an instrument, never a change.\n\nTWO CORRECTIONS TO THE ROUTED PREMISE, both measured here and both accepted by the orchestrator. The relayed \"777 insertions and ZERO deletions, purely additive\" does not reproduce: 2067 insertions and 42 deletions. And the deletions are a NARROWING -- adr.schema.json status went from anyOf[string, null] to an enum of three values -- so the committed schema was the LOOSER one and the pinned gate had been accepting adr status values the current profile rejects. That argued FOR the bump and removed the softening attached to it. The relayed figure appears to describe opum-cli-e2e own bundle, a different object.\n\nNOT DONE HERE, deliberately: the gate still cannot detect its own staleness, because lore check runs AT the pinned version and compares against what THAT version generates. Split to QCLI-358 with the argument carried verbatim, on the ruling that a network read inside a required context trades a false green for a flaky red.\n\nVersions this resolved against: workstation lore 0.8.0, CI lore 0.7.0 before and 0.8.0 after, quest 0.9.0.","blockers":[],"gates":[],"gateEvents":[]}
