{"id":"QCLI-223","aliases":[],"title":"Completing a task another active task depends on blocks all create/edit workspace-wide","status":"Done","description":"Discovered 2026-09-04 as a direct side effect of completing QCLI-220 while QCLI-221 depended on it: canonicalizeTaskLinks (task create) and createTaskLinkSession/canonicalizeTaskLinks (task edit, edit-batch) were only ever shown snapshot.tasks -- the ACTIVE-only task set -- when resolving dependency and parentId edges. The moment any active task depends on (or is parented under) a task that gets completed or archived, that edge becomes unresolvable, and EVERY subsequent task create and task edit in the whole workspace fails with dependency_target_not_found, not just operations touching the two tasks involved. Verified directly against this repository live: quest task edit on a wholly unrelated task (QCLI-1, QCLI-2) failed identically once QCLI-220 completed while QCLI-221 depended on it. This is a P0 -- every fleet session using quest against this tracker was blocked from writing to it.","priority":"high","type":"bug","acceptanceCriteria":[{"index":0,"text":"TaskService.create resolves dependency/parentId edges against every task location (tasks/completed/archive/tasks), not just active, while still reporting a specific task_already_exists (not a generic ambiguity) for a genuine id reuse against an existing record in any location","checked":true},{"index":1,"text":"TaskService.editOn resolves the edited task's own dependency/parentId edges the same cross-location way, without changing which tasks are directly editable (still active-only via findTask)","checked":true},{"index":2,"text":"TaskService.editBatch's graph-touching path (createTaskLinkSession) resolves against the same cross-location universe, not just the batch's active working set","checked":true},{"index":3,"text":"The auto-allocated-id concurrent-create race (task_already_exists surfacing from a sibling writer's just-committed id) still maps to the existing retryable conflict envelope at the CLI layer, matching how dependency_target_ambiguous already did","checked":true},{"index":4,"text":"A regression test proves: creating T2 depending on T1, completing T1, then creating/editing other tasks all still succeed (previously all failed)","checked":true},{"index":5,"text":"bun run check passes in full (typecheck, lint, format, layers, all tests)","checked":true}],"definitionOfDone":[],"plan":[],"implementationNotes":[],"comments":[],"labels":["bug","critical"],"documentation":[],"parentId":"QCLI-207","dependencies":[],"createdAt":"2026-09-04T03:16:51.447Z","updatedAt":"2026-09-04T03:17:05.413Z","finalSummary":"Fixed: create()/editOn()/editBatch() now resolve dependency/parentId edges against the full cross-location task set (this.taskRecords(snapshot)), not just active tasks, so a dependency on a completed/archived task no longer breaks every subsequent write. create() checks task_already_exists before canonicalization (not after) so explicit id reuse against a retired record reports the specific error, not a generic ambiguity; that error is now also mapped to the existing retryable conflict envelope at the CLI layer so the auto-id concurrent-create race still resolves the same way dependency_target_ambiguous always did. Verified live against this repo's own broken state (task edit on unrelated tasks failed identically before the fix, succeeded after), and via two new regression tests (cli-semantics.test.ts, cli-task-edit-batch.test.ts) confirmed to fail on the pre-fix code and pass on the fix. bun run check: 401/401 tests pass.","blockers":[],"gates":[],"gateEvents":[]}
