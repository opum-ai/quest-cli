{"id":"QCLI-317","aliases":[],"title":"promotion-fast-forward: the explicit fetch refspec is never asserted in the accept direction, and every refspec mutant dies only in one test about a different property","status":"Done","description":"Routed by opum-agent 2026-09-15: opum-doc measured that in their reference SHELL implementation the drop-refspec mutant SURVIVES the corrected fixture with 0 red, because the earlier missing-ref split emits the same diagnosis and exit code the shallow row asserts. lore-cli found the equivalent in its own .mjs -- deleting the workflow fetch line survived, and one wiring assertion fixed it. They asked this repository to confirm which mutant dies here and record it.\n\nMEASURED HERE, and the honest answer is in between: the mutants DO die, and they die for the wrong reason.\n\nTwo mutants against scripts/qualification/promotion-fast-forward.mjs, each run over the whole of test/qcli314-promotion-fast-forward.test.ts:\n  A  refspec +refs/heads/dev:refs/remotes/origin/dev  -->  bare \"dev\"        13 pass, 1 fail\n  B  refspec dropped entirely (git fetch --no-tags --quiet origin)           13 pass, 1 fail\n\nBOTH KILL THE SAME SINGLE TEST, and it is \"a remote with no dev blames the REMOTE and quotes git own error\" -- a test about DIAGNOSIS ATTRIBUTION, not about fetch wiring. It dies incidentally, and differently in each case:\n  A  git raw message becomes \"couldn not find remote ref dev\" instead of \"... refs/heads/dev\", so the assertion quoting git own text misses. The test is coupled to the refspec only because the refspec happens to appear inside the string git prints.\n  B  a bare git fetch origin SUCCEEDS against a remote that has no dev, so the REMOTE-blaming branch never executes at all and the run falls through to the missing-ref split.\n\nSo selective redness here is real but it is not evidence the refspec is under test. Nothing asserts the property the refspec exists for.\n\nWHY THE OBVIOUS CANDIDATE CANNOT COVER IT. The suite already has \"a missing origin/dev in a SHALLOW clone blames the workflow, not the remote\". That test builds the shallow fixture and then REMOVES THE REMOTE (git remote remove origin) with the comment that this reproduces the no-refspec outcome without mutating the script. That is true for the diagnosis, and it makes the test structurally blind to a refspec mutant: with no remote, the fetch never runs, so no mutation of the fetch line can change the result. The test cannot fail for this reason by construction.\n\nTHE PROPERTY THAT IS MISSING IS THE ACCEPT DIRECTION. The refspec exists so that origin/dev comes into existence as a consequence of THIS SCRIPT rather than of a clone configuration nobody controls from the workflow. Measured against the production shape -- git clone --depth 1 --branch main, remote INTACT, is-shallow true, remote.origin.fetch covering only main, origin/dev absent:\n  original, explicit refspec   exit 0, verdict emitted, and refs/remotes/origin/dev EXISTS after the run\n  mutant A                     exit 1, shallow diagnosis\n  mutant B                     exit 1, shallow diagnosis\nThe two mutants produce byte-identical output to each other, and that output is exactly what the existing shallow test already asserts -- which is the shape opum-doc reported. What separates the original from both mutants is not a better error, it is that the original SUCCEEDS.\n\nThis is the fleet rule in CLAUDE.md landing on its own guard: proving a gate rejects is half the proof and proving it accepts is the other half. QCLI-314 proved this file rejects, thoroughly and in many shapes. The one thing the refspec line does is make an accept POSSIBLE in a clone that would otherwise fail, and the accept direction is where it was never measured.\n\nThe fix is one test, not a redesign: keep the remote, assert exit 0 and the created ref.","acceptanceCriteria":[{"index":0,"text":"A test builds the production-shape fixture -- a shallow clone cut as git clone --depth 1 --branch main with the remote LEFT INTACT -- and asserts the run is ACCEPTED (exit 0, verdict on stdout) and that refs/remotes/origin/dev exists afterwards, so the refspec job is measured in the accept direction rather than through a diagnosis string","checked":true},{"index":1,"text":"Both refspec mutants (bare dev, and no refspec at all) are run against the new test and both turn it RED, recorded as two named runs with their exit codes and messages; the rest of the suite verdicts are unchanged by the addition","checked":true},{"index":2,"text":"The record states which test killed each mutant BEFORE this change and why that coupling was incidental, and why the existing shallow no-dev test is structurally incapable of covering the refspec","checked":true}],"definitionOfDone":[],"plan":["1. Add a production-shape fixture helper: git clone --depth 1 --branch main from the scratch origin, remote left INTACT, asserting the preconditions (is-shallow true, remote.origin.fetch covering only main, origin/dev absent) so the fixture cannot silently stop being the shape under test","2. Assert the ACCEPT direction: BEFORE_SHA=ZERO (branch creation is the one accept path a depth-1 clone can reach, since any real previous HEAD is unfetchable there), exit 0, the verdict on stdout, and refs/remotes/origin/dev resolving afterwards","3. Run both refspec mutants against the new test and record both reds with their messages","4. Restore, run the full file plus the full suite, open a PR"],"implementationNotes":["IMPLEMENTED on fix/QCLI-317-refspec-accept-direction, PR #146 into dev, head 71ae129.\n\nTHE MUTATION MATRIX, four runs over test/qcli314-promotion-fast-forward.test.ts:\n  clean tree                                          15 pass / 0 fail\n  mutant A  refspec -> bare \"dev\"                     13 pass / 2 fail\n  mutant B  refspec dropped (git fetch origin)        13 pass / 2 fail\n  restored                                            15 pass / 0 fail\nFull bun run check on the committed tree: 522 pass / 0 fail, 60 files, exit 0. The three biome warnings it prints are pre-existing -- they flag the Actions ${{ }} syntax inside the older workflow-wiring test, not anything added here.\n\nBEFORE the change each mutant scored 13 pass / 1 fail, and the one death was the SAME test in both cases. After, each scores 13 pass / 2 fail: the new accept test dies alongside it. So the delta this change buys is exactly one test dying for the right reason, and the pre-existing incidental death is left in place rather than tidied away -- it is a real assertion about diagnosis attribution and it happens to also be refspec-sensitive, which is a property worth keeping and not worth relying on.\n\nWHAT THE NEW TEST ASSERTS, in the order it matters:\n  preconditions   is-shallow true, remote.origin.fetch is exactly +refs/heads/main:refs/remotes/origin/main, and refs/remotes/origin/dev is ABSENT before the run\n  the accept      exit 0, the verdict on stdout, and no ::error:: of any kind\n  the consequence refs/remotes/origin/dev resolves to c4 AFTER the run\nThe preconditions are three assertions rather than a comment on purpose. The existing shallow test in this same file was already caught once by a fixture that had quietly stopped being the production shape (cloning --branch dev created origin/dev for free, making the case unfailable), and a fixture that cannot fail is the failure mode this whole file exists to be suspicious of.\n\nWHY BEFORE_SHA IS ZERO. Branch creation is the only accept path a --depth 1 clone can reach at all: any real previous HEAD is unfetchable there, which is precisely what the neighbouring shallow test asserts. Deepening the clone to get a richer accept would have destroyed the property under test. The assertion is about the FETCH, so assertion 2 is taken out of the picture deliberately rather than worked around.\n\nTHE FINDING TO SEND BACK TO opum-doc AND lore-cli, because it is not quite either of their results. Their mutants SURVIVED; mine DIED. Reporting only that would have closed the thread with a false all-clear in both directions -- they would conclude quest-cli was covered, and I would conclude the same. The truth is that selective redness and asserted-ness are different properties, and a mutation matrix reports the first while everyone reads it as the second. A mutant that dies in a test whose NAME has nothing to do with the mutated line is a signal to open that test, not a pass.\n\nThat generalises past this file: the value of a mutation matrix is in WHICH test died, and the matrix format -- a column of counts -- discards exactly that. Both of the earlier matrices on QCLI-314 recorded counts per clause and would have looked identical whether the deaths were principled or incidental.","PROCESS SLIP ON THIS RECORD, recorded because the warning that caught it is one this repository built.\n\nI ran task complete with all three acceptance criteria still unchecked. They were each satisfied by evidence already in the record -- the accept test exists and was run, both mutants scored 13 pass / 2 fail, and the note names which test killed each mutant before the change -- so the verdict is unaffected. The defect was in the order of operations: I wrote the final summary and completed in one chained command, and never ran the check step.\n\nQCLI-252 WORKED EXACTLY AS DESIGNED AND I MISREAD IT. complete printed the stderr warning naming the three unresolved items and added unresolvedAtCompletion to the JSON result. Because I had piped the command with 2>&1 into a JSON parser, the warning line landed in front of the envelope and python reported \"Extra data: line 2 column 1\". I read that as a malformed-output problem and went looking at the record instead of at the message -- which is how I found it, but by luck rather than by reading what I was told.\n\nTWO THINGS WORTH KEEPING. First, DEC-1 chose advisory-but-not-silent over enforcement, and this is a live instance of that choice paying: an enforced refusal would have blocked a completion that was substantively correct, while the warning cost nothing and still surfaced the slip. Second, the cost was self-inflicted by tooling: piping 2>&1 into a strict JSON parser turns a deliberate warning into what looks like a corrupt envelope. Parse stdout alone and read stderr separately, or the one signal designed to be noticed becomes indistinguishable from a bug in the thing emitting it.","SCOPING CORRECTION from opum-agent, relayed from opum-doc (ODOC-217, PR #303). Verified here against my own fixtures before accepting, and it holds.\n\nTHE OVERCLAIM. This record and PR #146 said, in effect, that what separates the original from the refspec mutants \"is not a better error, it is that it succeeds\" -- and the test docblock generalised it to \"the assertion has to be that the run SUCCEEDS where it otherwise could not\". Stated that way it is a claim about the LINE. It is actually a claim about the FIXTURE.\n\nVERIFIED IN MY OWN SUITE, which is what makes this worth recording rather than just accepting. My script DOES separate in the reject direction, in the remote-with-no-dev full-clone fixture: original and mutant A both exit 1 and both emit the REMOTE-blaming error, differing ONLY in the git text they quote back -- \"refs/heads/dev\" versus \"dev\". That is reject-direction separation by wording, and it is the same incidental coupling this record already described. I documented the coupling and then still wrote the general claim, which is the thing to notice.\n\nopum-doc fixture keeps the remote intact in its shallow row (clone --depth 1 file://origin --branch main) where mine removes it, and there the variants separate by diagnosis wording rather than by accept. So: DIRECTION OF SEPARABILITY IS A PROPERTY OF THE FIXTURE, NOT OF THE LINE. Their formulation, and it is better than mine.\n\nWHAT SURVIVES UNCHANGED, and it is the load-bearing half: coverage resting on one negative assertion tied to MESSAGE WORDING can be deleted by a rewording without any row going red. Whoever reworded git output, or reworded the script own diagnosis, would silently remove the only thing holding that line. That is why the accept assertion earns its place here -- not because accept is the only direction that can separate, but because it does not depend on wording at all.\n\nThe asserted-ness-versus-redness finding stands as stated; opum-agent confirms it was not affected.\n\nTHE PATTERN THIS MAKES, third instance tonight and the sharpest: a claim that is TRUE OF THE THING I MEASURED, stated as though it were true of the thing I was reasoning about. The CLAUDE.md name-the-object rule covers exactly this and I did not apply it -- I measured \"what separates original from mutant IN THIS FIXTURE SET\" and claimed \"what separates original from mutant\". One clause of scope, absent."],"comments":[],"labels":["bug","ci","test"],"documentation":[],"dependencies":[],"createdAt":"2026-09-15T21:46:15.593Z","updatedAt":"2026-09-15T22:03:01.508Z","finalSummary":"The explicit fetch refspec in scripts/qualification/promotion-fast-forward.mjs is now asserted in the ACCEPT direction. Landed as PR #146, squash-merged to dev as 84591ba.\n\nWHAT WAS ACTUALLY WRONG, which is not what the routed report predicted. opum-doc measured their reference shell implementation SURVIVING the drop-refspec mutant; here both mutants died, which read as already-covered. They were not. Both killed the same single test -- one about diagnosis ATTRIBUTION -- and each killed it incidentally: the bare form only because git quotes the refspec back inside its own error text, the dropped form only because a bare git fetch origin SUCCEEDS against a remote with no dev, so the REMOTE-blaming branch never executes. The neighbouring shallow no-dev test cannot cover it either: it REMOVES the remote, so the fetch never runs and no mutation of that line can change its result.\n\nVERIFIED as four runs over test/qcli314-promotion-fast-forward.test.ts:\n  clean                                   15 pass / 0 fail\n  mutant A  refspec -> bare \"dev\"         13 pass / 2 fail\n  mutant B  refspec dropped entirely      13 pass / 2 fail\n  restored                                15 pass / 0 fail\nFull bun run check on the committed tree: 522 pass / 0 fail, exit 0. Against the production shape (git clone --depth 1 --branch main, remote intact) the original exits 0 and creates refs/remotes/origin/dev, while both mutants exit 1 with byte-identical shallow diagnoses -- so what separates the original is not a better error, it is that it succeeds.\n\nThe new test asserts its fixture preconditions (is-shallow, the single-branch remote.origin.fetch, origin/dev absent) as three assertions rather than a comment, because this file was already caught once by a fixture that had quietly stopped being the production shape, and a fixture that cannot fail is the failure mode the whole file exists to be suspicious of.\n\nTHE FINDING WORTH MORE THAN THE FIX, routed to opum-agent and relayed to opum-doc and lore-cli: selective redness and asserted-ness are different properties, and a mutation matrix reports the first while everyone reads it as the second. A mutant dying in a test whose NAME has nothing to do with the mutated line is a signal to open that test, not a pass. The matrix format -- a column of counts -- discards exactly the information that distinguishes the two.","blockers":[],"gates":[],"gateEvents":[]}
