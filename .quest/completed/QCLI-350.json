{"id":"QCLI-350","aliases":[],"title":"The publisher says \"published and verified\" about the wrapper while verifying the platform packages, and names only two of three visibility states","status":"Done","description":"Two findings from the 0.9.0 publish, both about the publisher describing an object other than the one it measured.\n\nONE: the success line names the wrong object. publish-release.mjs prints\n\n  @opum-ai/quest 0.9.0 published and verified (1 check).\n\nfrom the result of waitForPublished(receipt, version). That helper verifies the SIX PLATFORM PACKAGES against the receipt digests -- it takes the receipt, and the receipt binds the platforms. It does not assert that the wrapper is readable from the registry. So the sentence attributes two different facts about two different objects to the wrapper: that its own publish call returned success, and that the platforms match the receipt.\n\nMeasured: the line printed with a genuine exit 0 while two independent anonymous reads disagreed -- a cache-busted packument had no 0.9.0 and https://registry.npmjs.org/@opum-ai%2Fquest/0.9.0 returned HTTP 404. The wrapper appeared about 60 seconds later, so the claim became true; it was not true when printed, and nothing in the output would have distinguished that from a wrapper that never landed.\n\nTWO: staged, never-landed and SLOW are three states, and both the tool and the runbook name two. When quest-linux-arm64 missed the visibility window the publisher reported absent-or-staged and advised --diagnose-staged to separate them. Neither was the answer. The package had landed and was merely slow: --diagnose-staged reported skip, already on the registry at this version. Its evidence was the exact signature the runbook attributes to the 0.7.0 staged incident -- absent from versions, absent from time, time.modified unchanged since the previous release -- so the strongest available read pointed confidently at the wrong one of three states. npm stage list could not adjudicate either, returning E401 for the publishing token.\n\nWHAT IS NOT BROKEN, and should not be changed while fixing this: the gate itself behaved exactly as designed. With one platform package unresolvable the wrapper was withheld, so nothing advertised an optionalDependency that could not resolve. That is the 0.7.0 consumer-facing failure being prevented. The defect is in what the tool SAYS, not in what it does.","priority":"medium","type":"bug","acceptanceCriteria":[{"index":0,"text":"The success line distinguishes what was verified from what was published: either it verifies the wrapper is readable before claiming it, or it names the platform packages as the object it verified.","checked":true},{"index":1,"text":"The unresolved-package diagnosis names SLOW as a third possibility alongside staged and never-landed, rather than presenting a binary whose evidence cannot separate the three.","checked":true},{"index":2,"text":"The runbook section on the 0.7.0 staged incident records that the same signature -- absent from versions, absent from time, time.modified unchanged -- was produced by mere propagation lag on 2026-09-18, so a future reader does not treat it as decisive for staging.","checked":true},{"index":3,"text":"Whatever changes, the wrapper is still withheld whenever any platform package does not resolve for a consumer.","checked":true}],"definitionOfDone":[],"plan":["1. Finding ONE (partly overtaken by QCLI-368, which added an npm-view dist.integrity comparison of all seven tarballs before the success line): after that, run the same anonymous consumer read the platform gate uses (waitForConsumerVisibility) on the wrapper alone, and print success only once it resolves. The line names each object and its check separately: platforms against the receipt (N checks), seven tarballs byte-identical, wrapper resolves for a consumer. If the wrapper does not resolve in the window, print a non-success message with its per-package state, and do not print the success line.","2. Finding TWO: describeVersionState for absent-or-staged names three possibilities (slow, staged, never landed), says to re-read before the 409 probe because slow clears on its own, and cites the 2026-09-18 linux-arm64 case. Keep the state string, which require-platform-visibility and the qcli300 tests consume. Update the publisher hint and the timed-out waitForPublished message the same way.","3. Runbook: the 0.7.0 STAGED section records that the same signature came from propagation lag on 2026-09-18.","4. AC4 is untouched: publishPlatformsThenWrapper and its gate do not change; the existing gate tests stay green.","5. Tests for the wording and for the wrapper-visibility step, plus a mutation check."],"implementationNotes":[],"comments":[],"labels":["release"],"documentation":[],"dependencies":[],"createdAt":"2026-09-19T00:39:15.472Z","updatedAt":"2026-09-25T16:30:02.331Z","finalSummary":"ONE: the success line printed \"@opum-ai/quest <v> published and verified (N checks)\" from waitForPublished, which checks the six PLATFORM packages against the receipt. QCLI-368 had since added an npm-view dist.integrity comparison of all seven tarballs, but that goes through the publishing client, which reads early. The script now also runs waitForConsumerVisibility on the wrapper, the same anonymous read that gates the platforms, and prints nothing until it resolves. If it does not, the script reports the wrapper state, says not to unpublish, and exits 1. describeVerifiedRelease lists each object beside its check. TWO: the absent-or-staged description names SLOW / STAGED / NEVER LANDED, cites the 2026-09-18 linux-arm64 case, and says to re-read before --diagnose-staged; the publisher hint and the timed-out message match. The state string is kept because CI and tests consume it. AC3: the runbook STAGED section already carried the 2026-09-18 amendment; a note now says the tool reports it. AC4: publishPlatformsThenWrapper and its gate are untouched (no hunk in the diff); the qcli300 and gate tests pass. VERIFIED: bun run check exit 0 (655 pass), lore check exit 0; mutation restoring the old line and the old two-state wording turned exactly the 2 new tests red (70/2), restored 72/72. NOT unit-tested: the wrapper-visibility call inside main(), which has no seam. Delivered in opum-ai/quest-cli#273 on source commit 3314c2d. Concluded runs there: Prepublication qualification 36160681382 (push) and 36160687462 (pull_request), Tracker integrity 36160687437, lore check 36160687457, all success.","blockers":[],"gates":[],"gateEvents":[]}
