{"id":"QCLI-163","aliases":["backlog:/Volumes/external/repos/quest-cli:active:QCLI-163","QCLI-163"],"title":"task create --dependency forces topological-order creation; forward references fail with dependency_target_not_found","status":"Done","summary":"{\"backlog\":{\"sourcePath\":\"backlog/tasks/qcli-163 - task-create-dependency-forces-topological-order-creation-forward-references-fail-with-dependency_target_not_found.md\",\"sourceFolder\":\"active\",\"git\":{\"commit\":\"72fe937a8bc894dd562871d6a13cec42d162b848\",\"blob\":\"d4c0eedd89bf3f2bc1f11780eda90348da88e67c\"},\"assignees\":[],\"references\":[\"src/application/tasks/tasks.ts\",\"src/domain/tasks/tasks.ts\",\"docs/specs/quest-cli-dependency-ready-set-and-blocking-design.md\"],\"modifiedFiles\":[],\"createdAt\":\"2026-09-02 20:07\"}}","description":"---\nid: QCLI-163\ntitle: >-\n  task create --dependency forces topological-order creation; forward references\n  fail with dependency_target_not_found\nstatus: To Do\nassignee: []\ncreated_date: '2026-09-02 20:07'\nlabels:\n  - cli\n  - dx\n  - dependencies\ndependencies: []\nreferences:\n  - src/application/tasks/tasks.ts\n  - src/domain/tasks/tasks.ts\n  - docs/specs/quest-cli-dependency-ready-set-and-blocking-design.md\npriority: low\ntype: enhancement\nordinal: 192000\n---\n\n## Description\n\n<!-- SECTION:DESCRIPTION:BEGIN -->\nReported via opag/ISSUES.md, verified against source 2026-09-02: task create's dependency validation runs canonicalizeTaskLinks([...tasks, newTask]) synchronously at creation (src/application/tasks/tasks.ts:401), which throws dependency_target_not_found (src/domain/tasks/tasks.ts:526) when --dependency/--dep names a task that doesn't exist yet. This is a deliberate, ratified design choice (QCLI-62 AC1/AC3: named missing-target errors, fail-closed validation), not a bug -- confirmed by reading docs/specs/quest-cli-dependency-ready-set-and-blocking-design.md. The friction is real though: an agent creating tasks with a forward dependency (e.g. EDK-20 depending on not-yet-created EDK-22) hits an unhelpful stop and must either reorder creation or fall back to a two-pass create-then-task-edit-add-dependency workflow, discovering that pattern by trial and error.\n<!-- SECTION:DESCRIPTION:END -->\n\n## Acceptance Criteria\n<!-- AC:BEGIN -->\n- [ ] #1 The task-creation guide (backlog instructions task-creation, or quest's own equivalent guide) explicitly documents that dependency targets must already exist and shows the two-pass create-then-add-dependency workaround for forward references\n- [ ] #2 dependency_target_not_found's error message/hint points the caller at the two-pass workaround instead of stopping with no next step\n- [ ] #3 Explicitly re-affirm or revisit the QCLI-62 fail-closed ratification for this specific case before changing validation behavior -- this is a UX/docs task by default, not a request to relax validation\n<!-- AC:END -->\n","priority":"low","type":"enhancement","ordinal":192000,"acceptanceCriteria":[{"index":0,"text":"The task-creation guide (backlog instructions task-creation, or quest's own equivalent guide) explicitly documents that dependency targets must already exist and shows the two-pass create-then-add-dependency workaround for forward references","checked":true},{"index":1,"text":"dependency_target_not_found's error message/hint points the caller at the two-pass workaround instead of stopping with no next step","checked":true},{"index":2,"text":"Explicitly re-affirm or revisit the QCLI-62 fail-closed ratification for this specific case before changing validation behavior -- this is a UX/docs task by default, not a request to relax validation","checked":true}],"definitionOfDone":[],"plan":["Add a hint to dependency_target_not_found in src/cli/main.ts's error-handling catch block, pointing at the two-pass create-then-add-dependency/--add-dependency or --parent workaround. Keep the raw message string unchanged (two existing cli-semantics.test.ts tests already assert it verbatim with toMatchObject, and this is a docs/UX task per AC3, not a validation-behavior change).","Add a Dependencies must already exist subsection to the task-creation guide in src/application/agents/guides.ts, showing the two-pass workaround explicitly.","Re-affirm QCLI-62's fail-closed ratification for this specific case in the task's own notes/final-summary (AC3) -- explicitly confirm no validation behavior changes.","Add/extend a regression test asserting the hint text on a forward-reference dependency_target_not_found."],"implementationNotes":["AC1: added a 'Dependencies must already exist' section to the task-creation guide (src/application/agents/guides.ts) documenting that --dependency/--parent validate against real records at creation time, with the two-pass create-then-quest-task-edit---add-dependency workaround shown as an executable recipe. AC2: added a hint to dependency_target_not_found in src/cli/main.ts's error-handling catch block, pointing at the same two-pass workaround; kept the raw message string 'dependency_target_not_found' unchanged since two existing cli-semantics.test.ts tests already assert it verbatim via toMatchObject (adding a sibling hint field doesn't break them). AC3 (explicit re-affirmation, not a code change): QCLI-62's fail-closed ratification stands unchanged here. This task added a hint and documentation only -- no change to when/whether the validation throws, no deferred resolution, no relaxed check. A forward reference still fails synchronously at creation or at --add-dependency time exactly as before; the only difference is the caller is now told what to do next. Verified: bun run check 428/428 tests green (extended the two existing cli-semantics.test.ts assertions with a hint expectation, and updated guide-commands.test.ts's exact recipe count/placeholder map so the new guide recipe is itself exercised end-to-end against a real store, not just asserted in prose)."],"comments":[],"labels":["cli","dx","dependencies"],"documentation":[],"dependencies":[],"createdAt":"2026-09-02T20:07:00.000Z","updatedAt":"2026-09-07T17:44:43.977Z","finalSummary":"task create --dependency/--parent's fail-closed forward-reference validation (dependency_target_not_found) is deliberate per QCLI-62 and stays unchanged -- this was a UX/docs gap, not a bug. Fixed by: (1) a new 'Dependencies must already exist' section in the task-creation guide showing the two-pass create-then-add-dependency workaround as an executable recipe; (2) a hint on dependency_target_not_found in the CLI's error handler pointing at the same workaround, without changing the error's message string (two existing tests assert it verbatim). Regression coverage: extended both existing cli-semantics.test.ts assertions of dependency_target_not_found to also check the hint text, and updated guide-commands.test.ts's exact recipe count and placeholder map so the new guide recipe runs for real against a test store (not just asserted as prose) -- caught and fixed an off-by-one in my own placeholder mapping (the guide's pre-existing first recipe already consumes one task id) exactly because that test executes the recipe rather than trusting it. bun run check: 428/428 tests green.","blockers":[],"gates":[],"gateEvents":[],"source":{"system":"backlog","reference":"QCLI-163"}}
