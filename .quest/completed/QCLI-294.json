{"id":"QCLI-294","aliases":[],"title":"Release quest-cli 0.7.0 (paired with lore 0.7.0)","status":"Done","description":"Bump quest-cli from 0.6.2 to 0.7.0, paired in lockstep with lore-cli 0.7.0 (both parked at 0.6.2 as of the pre-tag audit, OPAG-142). Landing the version bump on dev/main is ordinary delivery; the tag and the publish are separate, later, user-authorized steps -- this task covers the bump only.\n\nVerified before starting: npm holds no 0.7.0 for any of the seven @opum-ai/quest* package names (checked live). Bun 1.3.14 matches package.jsons pinned packageManager.\n\nFile list, per docs/runbooks/quest-cli-package-and-release.md#what-a-version-bump-touches (written 2026-09-14 from a dedicated pre-tag audit) plus one gap that audit missed and this task found: .claude-plugin/plugin.json (0.6.0, unchanged since the 2026-09-10 recreation, stuck two releases behind the CLI it ships alongside -- QCLI-258 the 0.6.0 release task already listed it as a hand-edited site, and nothing enforces it, so it silently drifted through 0.6.1 and 0.6.2).","acceptanceCriteria":[{"index":0,"text":"Version bumped across every hand-edited location (package.json, QUEST_VERSION, QUEST_ADAPTER_PINNED_VERSION, the conformance fixture, .claude-plugin/plugin.json) and all six platform packages rebuilt via a CI dispatch of prepublication-qualification.yml against the release branch (not a local build-platform-packages.mjs run), with check:packages/test:packages both green, landed as dev-only prep -- no tag, no promotion beyond dev->main, no publish yet","checked":true},{"index":1,"text":"bun.lock regenerated so its platform-package optionalDependencies pins match the new version (QCLI-292s subject, touched here regardless of that tasks own resolution)","checked":true},{"index":2,"text":"CLAUDE.md managed Quest block regenerated against the freshly built local binary, not the globally installed CLI","checked":true},{"index":3,"text":"CHANGELOG.md Unreleased heading moved to the new version with todays date, not before this bump actually lands","checked":true},{"index":4,"text":"dev promoted to main via the standard fast-forward sequence once the bump PR is green","checked":true}],"definitionOfDone":[],"plan":["1. Create branch chore/QCLI-294-release-0.7.0 off dev","2. Bump the four independent hand-edited sources plus .claude-plugin/plugin.json to 0.7.0, and hand-sync root optionalDependencies + each platform package.json .version field (text only, binaries untouched) so check:packages passes; commit","3. Push and dispatch prepublication-qualification.yml against the branch to build real platform binaries in CI (no local build-platform-packages.mjs run)","4. Download the six quest-package-<target> artifacts, replace npm/quest-<platform>/{package.json,bin/quest(.exe),LICENSE}, update root package.json questPlatformPackages checksums","5. Regenerate bun.lock via bun install; regenerate CLAUDE.md managed block against the freshly built local darwin-arm64 binary (execute before final delete-and-recreate per the SIGKILL hazard runbook section)","6. Move CHANGELOG.md Unreleased heading to 0.7.0 with todays date","7. bun run check && check:packages && test:packages all green; commit","8. PR into dev, wait for source-gates + Tracker integrity green, squash-merge","9. Promote dev to main via the standard fast-forward sequence","10. Report; do not tag or dispatch release.yml"],"implementationNotes":["HANDOFF at context-budget recycle, 2026-09-15. Branch chore/QCLI-294-release-0.7.0, clean and pushed at 7a5d3de. No tag, no release.yml, nothing published.\n\nCORRECTION TO THE RECYCLE BRIEF FIRST, because acting on it as written wastes a cycle: the qualification run cited in the recycle instruction (34946220664) FAILED and is dead. The LIVE run is 34946440591, dispatched against 13386e6, in_progress at handoff with source-gates running. Watch that one. Also, bun.lock is already DONE (commit 13386e6), not pending in stage two as the brief listed.\n\nSTAGE ONE, DONE AND PUSHED (four commits):\n- 00d1318 tracker task file, kept separate so the release branch history stays reviewable (QCLI-271s recorded lesson about tracker notes on release branches).\n- cd338ac six version-string sites to 0.7.0: package.json .version, its six optionalDependencies pins, src/application/version.ts QUEST_VERSION, src/contract/tracker/index.ts QUEST_ADAPTER_PINNED_VERSION, fixtures/tracker/v1/conformance.json questVersion, .claude-plugin/plugin.json .version, plus each of the six npm/quest-<platform>/package.json .version fields.\n- 13386e6 bun.lock regenerated.\n- 7a5d3de CHANGELOG heading moved to 0.7.0 dated 2026-09-15, plus two runbook corrections.\n\nTHE ORDERING, VERIFIED NOT ASSUMED -- STRINGS FIRST, BYTES SECOND. check-package-artifacts.mjs tests version-string agreement and checksum-to-bytes agreement as INDEPENDENT conditions. So a commit can move every version string while leaving the binaries, every questBinarySha256, and package.jsons questPlatformPackages map untouched and still self-consistent with the old bytes. That state passes check:packages (verified exit 0), which keeps source-gates green, which is the ONLY thing that lets immutable-candidates (needs: source-gates) run and build the real binaries. Bump only the root version and the gate that would let CI fix it is the gate the mismatch breaks. This reconciles a real contradiction between the runbooks Prerequisites bullet and its What-a-version-bump-touches section; both are now corrected in 7a5d3de.\n\nbun.lock belongs in the STRINGS commit, learned the hard way: run 34946220664 failed at source-gates very first step with \"lockfile had changes, but lockfile is frozen\", ahead of typecheck, tests and check:packages. What bun install produced is correct and will look alarming if unexpected: the six requirement pins moved to 0.7.0 AND the six resolved entries were dropped entirely, integrity hashes and all, because @opum-ai/quest-*@0.7.0 does not exist on the registry to resolve. They come back on the first bun install AFTER publication -- exactly what commit 101b265 did for 0.6.2. A lockfile with requirement pins and no resolved entries for these six is the correct between-bump-and-publish state.\n\nAUDIT GAP FOUND: .claude-plugin/plugin.json is a sixth hand-edited version site the pre-tag audit missed, and it was already stale -- reading 0.6.0 since the 2026-09-10 recreation, wrong through the abortive 0.6.1 and through the 0.6.2 that shipped. QCLI-258 listed it once; the knowledge was lost. Nothing generates it, no test compares it to package.json. Now in the runbook list.\n\nSTAGE TWO, NOT STARTED, IN ORDER:\n1. Wait for run 34946440591 to go green (six immutable-candidates matrix jobs plus source-gates). If source-gates fails again, read the log before changing anything -- both failures so far were real and specific.\n2. gh run download 34946440591 --name quest-package-<target> for all six targets (darwin-arm64, darwin-x64, linux-arm64, linux-x64, win32-arm64, win32-x64) into a scratch dir. KEEP those downloaded bytes untouched as the source of truth; you need them again at step 6.\n3. Replace npm/quest-<target>/{package.json,bin/quest(.exe),LICENSE} from each artifact. chmod +x all six binaries including both .exe.\n4. Update package.jsons questPlatformPackages checksum map to the six new digests (each artifacts own package.json carries its questBinarySha256; they must agree).\n5. Regenerate CLAUDE.mds managed Quest block: ./npm/quest-darwin-arm64/bin/quest agents --update-instructions --target claude -- the FRESHLY BUILT LOCAL binary, never the globally installed CLI on PATH, which reads its own running version and will silently write 0.6.2 back while reporting state: current. That exact mistake happened at 0.6.1.\n6. SIGKILL HAZARD, and step 5 just triggered it. Executing the darwin-arm64 Mach-O taints its vnode, so the next git operation that mmaps it gets SIGKILLed (exit 137) -- and that takes down unscoped git status/add/commit on unrelated paths too. Do all execution you need FIRST (steps 5 and 7), then as the LITERAL LAST action before any git command: rm npm/quest-darwin-arm64/bin/quest and recreate it from the untouched step-2 download (fresh inode), with ZERO execution between that recreate and the commit. Runbook section \"A locally-rebuilt darwin-arm64 binary can SIGKILL git on this host\" has the escape hatches if it bites anyway.\n7. bun run check && bun run check:packages && bun run test:packages, all exit 0. (Execution happens here, so this is before step 6s recreate.)\n8. Commit stage two, push, open PR into dev. Wait for source-gates AND Tracker integrity green ON THE EXACT HEAD SHA -- read them by name via gh api repos/opum-ai/quest-cli/commits/<sha>/check-runs, not the PR rollup. Squash-merge.\n9. Promote dev to main: open a dev->main PR, confirm those same two required contexts green on that exact SHA, then git push origin origin/dev:main (the explicit remote-tracking ref, never bare dev). promotion-is-manual will report FAILURE and mergeStateStatus will read UNSTABLE -- both are the always-fail tripwire working as designed, not blockers. Never the merge button.\n10. STOP. Do NOT create the tag and do NOT dispatch release.yml. The tag and publish happen together at the users keyboard; lore-cli bumps to 0.7.0 in lockstep and the orchestrator sequences the pair once both report. Report the bump with every site named.\n\nLocal checkout: branch chore/QCLI-294-release-0.7.0, working tree clean, HEAD equals origin at 7a5d3de.","Post-handoff confirmation, 2026-09-15: the bun.lock fix WORKED, verified rather than assumed. Run 34946440591 source-gates is completed/success, and all six immutable-candidates matrix jobs (darwin-arm64, darwin-x64, linux-arm64, linux-x64, win32-arm64, win32-x64) are now in_progress -- which is the proof, since immutable-candidates declares needs: source-gates and cannot start until that gate is green.\n\nSo the strings-first-bytes-second ordering is confirmed end to end: version strings synced (including bun.lock) pass check:packages and the frozen-lockfile install, source-gates goes green, and CI proceeds to build the real binaries. That was the whole open question at handoff time.\n\nSuccessor: stage two step 1 is now just waiting for those six jobs to finish, then downloading the artifacts. Nothing about the sequence has changed.","STAGE TWO DONE, commit 5ba34f8 on chore/QCLI-294-release-0.7.0. Everything below was verified, not assumed.\n\nARTIFACTS. Qualification run 34946440591 (dispatched against 13386e6) finished green: source-gates plus all six immutable-candidates matrix jobs plus candidate-bundle. Downloaded all six quest-package-<target> artifacts. Before installing anything, recomputed sha256 over each downloaded binary and compared it to that same artifacts own questBinarySha256 -- six of six agreed, so the bytes CI hashed are the bytes that arrived. Installed package.json, LICENSE and bin/quest(.exe) into npm/quest-<target>/, chmod +x on all six including both .exe, then wrote the same six digests into package.jsons questPlatformPackages map. LICENSE came out byte-identical everywhere, so it does not appear in the diff; that is expected, not a missed copy.\n\nCLAUDE.MD. Regenerated with ./npm/quest-darwin-arm64/bin/quest agents --update-instructions --target claude. Confirmed the local binary reports 0.7.0 before running it, and confirmed the block now reads 0.7.0 after. The pre-state was worth recording: agents --check against the local binary returned state: version-only, exit 0, message \"block records 0.6.2; the installed CLI is 0.7.0. Content is otherwise current\" -- so the CI gate would NOT have caught a skipped regeneration here, by design. That is the whole reason this is a manual step.\n\nTHE SIGKILL HAZARD FIRED, EXACTLY AS THE RUNBOOK PREDICTS. The git diff --stat run immediately after the CLAUDE.md regeneration died with exit 137 while the quest command in the same shell had already returned 0. Successors: that is not a failed regeneration and not a corrupt repo, it is the tainted vnode, and the recovery is the documented one. Held off every git command until after the recreate; deleted and re-copied npm/quest-darwin-arm64/bin/quest from the untouched download as the literal last action, verified the new inode and that its sha256 still matched the manifest, and git worked first try with zero retries.\n\nSUITES, exit codes captured individually rather than chained, because zsh does not populate PIPESTATUS the way bash does and a piped \"| tail\" had already swallowed one: bun run check exit 0 (489 pass, 0 fail, 57 files), bun run check:packages exit 0, bun run test:packages exit 0. One pre-existing biome lint WARNING (test/qcli122-fourth-pass.test.ts:374, unused catch binding) rides along; it is a warning not an error, biome ci passes, check exits 0, and it predates this branch -- not introduced here and not fixed here.\n\nAC5 stays unchecked: dev to main promotion has not happened yet.","PROMOTION DONE. dev 505d2a4 fast-forwarded to main; PR #100 auto-marked MERGED, no merge commit created.\n\nGate evidence, read by name on the exact SHA rather than off the PR rollup. On c7a332a (PR #99 head): source-gates x2 (run ids 104313476752 and 104313552907) plus Tracker integrity, all completed/success -- the double source-gates is the documented source-touching double-fire, pull_request plus the path-filtered push trigger, and waiting on only one of them would have been waiting on half the gate. On 505d2a4 (dev head, PR #100): source-gates x2 and Tracker integrity x2, four of four completed/success, because the merge-to-dev push and the dev-to-main pull_request each fire their own set. Confirmed origin/main was an ancestor of origin/dev before pushing, and pushed origin/dev:main -- the remote-tracking ref, never bare dev, which can silently no-op off a stale local branch.\n\nA SIDE BENEFIT WORTH RECORDING, because it is stronger evidence than anything the checksums give: the six native-execution jobs (macos-14 arm64, macos-15 x64, ubuntu-24.04 arm64/x64, windows-11 arm64, windows-2022 x64) all went green on the PR. Those EXECUTE the newly installed binaries on real hardware per platform. So the six artifacts are not merely byte-identical to what CI hashed, they actually run on the OS/arch each one claims. Checksum agreement proves delivery; those jobs prove the thing delivered works.\n\nTAG AND PUBLISH STATE, verified after the promotion rather than assumed: git ls-remote --tags origin lists v0.6.0, v0.6.1, v0.6.2 and NO v0.7.0. No release.yml dispatch, nothing published to npm. That line holds until the user authorizes it, sequenced with lore-cli 0.7.0.\n\nTWO REPOSITORY-PROFILE FACTS HAVE DRIFTED SINCE 2026-09-11 and someone should correct the profile block, though not on my say-so and not in this task. The profile records, from the 2026-09-10 deletion and recreation, that the tags API listed only v0.6.0 and the releases API returned []. Neither is true now: all three of v0.6.0/v0.6.1/v0.6.2 resolve, and the releases API returns 1. I did not create either -- I created no tag and no release, and the tag list was already in this state before the promotion. This matters to quest-web, whose npm run check fetches every external link live and cites this repository, and which was advised to treat these tags as weaker anchors than the immutable npm version. That advice may now be more conservative than it needs to be."],"comments":[],"labels":["release","packaging"],"documentation":[],"dependencies":[],"createdAt":"2026-09-15T08:14:29.154Z","updatedAt":"2026-09-15T08:51:14.398Z","finalSummary":"quest-cli is at 0.7.0 on dev and main. The version bump shipped; the tag and the publish deliberately did not, and that was the point of scoping this task to the bump alone.\n\nWHAT CHANGED. Six hand-edited version sites moved to 0.7.0: package.json .version and its six optionalDependencies pins, src/application/version.ts QUEST_VERSION, src/contract/tracker/index.ts QUEST_ADAPTER_PINNED_VERSION, fixtures/tracker/v1/conformance.json questVersion, .claude-plugin/plugin.json .version, and each npm/quest-<platform>/package.json .version. bun.lock regenerated. CHANGELOG Unreleased moved to 0.7.0 dated 2026-09-15. All six platform packages replaced with binaries built by prepublication-qualification run 34946440591, and CLAUDE.mds managed Quest block regenerated against the freshly built local binary.\n\nHOW IT WAS VERIFIED. Each downloaded binarys sha256 recomputed and matched against that artifacts own questBinarySha256 before installing, six of six. bun run check exit 0 (489 pass, 0 fail), check:packages exit 0, test:packages exit 0. Both required contexts read by name on each exact head SHA, including the duplicate instances: three source-gates runs and three Tracker integrity runs across the two SHAs, every one success. Six native-execution jobs executed the new binaries on real per-platform hardware and passed. main confirmed an ancestor of dev before a fast-forward push of origin/dev:main.\n\nTHREE THINGS THIS TASK LEARNED THAT THE RUNBOOK DID NOT ALREADY SAY.\n\n1. bun.lock belongs in the version-STRING commit, not the binary commit. A stale lockfile fails bun install --frozen-lockfile at the first step of source-gates, ahead of typecheck, tests and check:packages -- so the gate that would let CI build the corrected binaries is the gate the omission breaks. Cost one dead qualification run to find.\n\n2. .claude-plugin/plugin.json is a sixth hand-edited version site nothing checks against package.json. It had read 0.6.0 since the 2026-09-10 recreation -- stale through the abortive 0.6.1 and through the 0.6.2 that actually shipped. QCLI-258 named it once and the knowledge was lost, which is the argument for a check:packages rule rather than another runbook line. Filed as follow-up work, not done here.\n\n3. quest agents --check CANNOT catch a skipped CLAUDE.md regeneration. Against the local 0.7.0 binary it returned state: version-only, exit 0, by design. The manual step exists precisely because the automated gate is specified not to fail here. Anyone trusting CI to catch this will ship a stale block, which is what happened at 0.6.1.\n\nWHAT IS DEPENDENT ON THIS AND NOT DONE. No v0.7.0 tag exists and nothing is published; verified against the live remote after promotion. QCLI-289s remaining acceptance criterion is the 0.7.0 publish EVENT, so it cannot close until the user publishes and opum-cli-e2e reads the result with its shape-locked suite. The publish is paired with lore-cli 0.7.0 and sequenced by the orchestrator at the users keyboard.","blockers":[],"gates":[],"gateEvents":[]}
