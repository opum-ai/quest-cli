{"id":"QCLI-375","aliases":[],"title":"CI source-gates never runs 36 top-level test files or test/e2e/release -- a green PR says nothing about them","status":"Done","description":"Found 2026-09-25 while wiring QCLI-292. The required source-gates context runs scripts/qualification/prepublish.mjs, whose runSourceGates runs bun test on SEVEN paths only: test/domain, test/contract, test/integration, test/cli-tracker-process.test.ts, test/fault/git/local-git.test.ts, test/e2e/migration, test/scale. No workflow runs the full suite. Grep of .github/workflows for bun test finds only projection-platform.yml running one sqlite file. So `ls test/*.test.ts` (36 files at d739f60, including every qcliNNN regression test: qcli300 CI wrapper gate, qcli304, qcli314 promotion fast-forward, qcli346, qcli371 plugin tests, etc.) and all of test/e2e/release/ (publish-release, registry-visibility, e2e-receipt) run ONLY when someone runs bun run check locally. A PR that breaks any of them goes green in CI. OBJECT MEASURED: prepublish.mjs runSourceGates at this branch, and .github/workflows/*.yml. NOT MEASURED: whether any of those files currently fails (local bun run check passes with 660 tests, so none do today). QCLI-292 gated its own check by adding a dedicated lockfile_pins gate rather than relying on its test file, for exactly this reason.","priority":"medium","acceptanceCriteria":[{"index":0,"text":"Every test file under test/ is run by a required CI context, or is listed with a written reason for being excluded","checked":true},{"index":1,"text":"A test deliberately broken in a top-level test/*.test.ts file turns the required context red, and the same PR with the break reverted turns it green (two measurements)","checked":true},{"index":2,"text":"The prepublication qualification record still names each gate, so a failure says which suite broke","checked":true}],"definitionOfDone":[],"plan":["1. Replace the fixed seven-path loop in runSourceGates with named gates that cover every test file: keep the seven, add release (test/e2e/release) and top_level (each test/*.test.ts not already covered, enumerated at run time).","2. Add a test_coverage gate that enumerates every *.test.ts under test/ and fails naming any file no gate runs and no written exclusion covers, so a new directory cannot escape silently.","3. Measure locally: runtime of the newly-run files, and whether any needs network, macOS, or touches the darwin-arm64 binary.","4. Two CI measurements on the PR: a deliberately broken top-level test turns source-gates red naming the top_level gate; the revert turns it green. Cite run ids only once concluded on the final head.","5. Correct the workflow comment that claims source-gates runs the full test suite."],"implementationNotes":["HANDOFF on recycle, 2026-09-25, orchestrator-requested. THIS IS THE NEXT TASK: the orchestrator ruled it goes first for the successor. Not started, no branch. STATE (verified by fetch just now): origin/main == origin/dev == 86d931b, 0 open PRs. This session landed and promoted QCLI-349 (#270/#271), QCLI-350 (#273), QCLI-304 (#275), QCLI-292 (#277); promotion PRs #269 #272 #274 #276 #278. WHAT IS ESTABLISHED: source-gates runs scripts/qualification/prepublish.mjs, and runSourceGates runs `bun test` on 7 paths only. No workflow runs the full suite: grepping .github/workflows for bun test finds only projection-platform.yml running one sqlite file. So the top-level test/*.test.ts files (36) and test/e2e/release/ run only on a local `bun run check`. APPROACH I would take: add one more gate to runSourceGates (e.g. [\"top_level\", \"test/*.test.ts\"] plus [\"release\", \"test/e2e/release\"]), or run full `bun test` as a single gate. First measure its CI runtime, and whether any file needs network or macOS. The darwin-arm64 binary AMFI SIGKILL trap (CLAUDE.md profile) matters if a test hash-objects npm/. Prove it with TWO CI runs: a deliberately broken top-level test goes red, the revert goes green (AC2). LESSONS from this session: (1) cite a CI run id only after it concludes, on the final head; a later push cancels in-flight runs (see QCLI-349 correction, #271). (2) The trunk-guard hook reads HEAD before a compound command runs, so create the branch in its own command before committing. (3) A test file under test/ is NOT a CI gate by itself; that is this task. (4) When editing package.json with python json.dump, pass ensure_ascii=False or it escapes the em dash in description; prefer a sed line insert. SKILLS: opum-workflow:opum-sdlc, opum-quest:quest, opum-workflow:opum-handoff restore.","Built on fix/QCLI-375-full-suite-in-source-gates at 5f78811, PR opum-ai/quest-cli#281. Local, clean tree: prepublish.mjs with QUEST_QUALIFICATION_SOURCE=1 exit 0, 17 gates passed; test_coverage read 80 files, 36 top-level, 0 ungated, 0 empty gates. NEGATIVE CONTROL: an ungated test/probe/probe.test.ts made only test_coverage fail (exit 1) and named that file; removed afterwards. Runtime of the newly-run files locally: top_level (35 files, 197 tests) 48s, release (6 files, 65 tests) 4s. None needs network or macOS; registry and darwin references in them are injected fakes or string fixtures. No test reads npm/*/bin, so the AMFI trap does not apply. The workflow comment that says source-gates runs the full test suite is now accurate and was left as is. Also promoted 52b14af to main (opum-ai/quest-cli#280), and read origin/main back as 52b14af.","CI PROOF on PR opum-ai/quest-cli#281. (0) FINDING, the reason this task exists: the first CI run of top_level (push run 36168076402 on 5f78811) failed exactly one test, the QCLI-374 by-value removal test in test/cli-task-edit-if-revision.test.ts, which timed out at bun's 5000ms default. The pull_request run of the same SHA passed. It makes 40 CLI spawns and takes 3.1s locally; every other slow spawn loop in the newly-run files already declares a budget (checked the three over 1.5s in that log). Fixed at 6627c0f with the file's own 30_000 convention: push 36168750838 and pull_request 36168755340 both success. (1) AC2 BREAK, 33eac15, test/cli-scaffold.test.ts expecting a wrong program name: push 36169376891 and pull_request 36169383636 both failure, and in each record exactly one gate failed (top_level, naming the scaffold test) and 16 passed. (2) AC2 REVERT, 07f8358, tree identical to 6627c0f: push 36169999756 and pull_request 36170006959 both success, 17 of 17 gates passed. The CI record at 36170006959 shows test_coverage passed with filesRead 80, topLevel 36, ungated [], emptyGates []."],"comments":[],"labels":["ci","testing"],"documentation":[],"dependencies":[],"createdAt":"2026-09-25T17:03:25.781Z","updatedAt":"2026-09-25T18:00:01.830Z","finalSummary":"CHANGE: scripts/qualification/prepublish.mjs runSourceGates, which is the required source-gates context, now runs every test file under test/. Two gates are new: release (test/e2e/release, 6 files) and top_level (every test/*.test.ts that no other gate names, enumerated at run time; 35 files). A third new gate, test_coverage, reads every *.test.ts under test/. It fails on a nested file that no gate covers and that TEST_EXCLUSIONS does not name with a reason, on a gate path that matches nothing, and on zero files read. It reports how many files it read. Each suite is still its own named gate, so a failure says which one broke (AC3). FOUND BY THE CHANGE: the QCLI-374 by-value removal test had no time budget. It takes 3.1s locally and timed out at the 5s default on its first CI run. It now declares the file's own 30s budget. VERIFIED: locally, 17 of 17 gates passed. An ungated test/probe file made only test_coverage fail, and it named that file. In CI (PR opum-ai/quest-cli#281), a deliberate break in the top-level test/cli-scaffold.test.ts (33eac15) failed source-gates on both triggers, 36169376891 and 36169383636, with exactly one gate failed (top_level) and 16 passed. The revert (07f8358) passed on both, 36169999756 and 36170006959, with 17 of 17 gates. The CI record shows test_coverage with filesRead 80 and ungated [] (AC1, AC2). COST: about 53s more locally. NOT COVERED: scripts/test-repository-check-scope.mjs is part of bun run test but is not a test/ file, and source-gates still does not run it. That is outside this task's criteria and is noted rather than widened into it.","blockers":[],"gates":[],"gateEvents":[]}
